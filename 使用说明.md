# ThatPerson 使用说明（从部署到使用）

> 适用范围：npm 发布版 `@nineteenfolk/thatperson`（v1.2.0）
> 一句话：ThatPerson 是个人 AI 管家，对话式 CLI + 长期记忆 + 技能系统 + 出厂人格。

## 一、环境要求

- Node.js ≥ 22.13（当前 UI 依赖链为 ESM-only，Node 18/20 无法运行；开发验证环境 Node 24）。
- 真实对话需要 DeepSeek API Key；`--mock` 离线演示无需 Key。
- Windows PowerShell 建议使用 `npm.cmd`（本机执行策略 Restricted 会拦截 `npm.ps1`）。

## 二、部署（安装）

### 2.1 全局安装

```powershell
npm.cmd install -g @nineteenfolk/thatperson
```

### 2.2 验证安装

```powershell
thatperson --version     # 应输出 1.2.0
thatperson --help        # 应打印 CLI 帮助
```

> 若提示「无法加载 thatperson.ps1」：是 PowerShell 执行策略拦截。改用 `thatperson.cmd`，或执行
> `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 放开后即可直接使用 `thatperson`。

### 2.3 检查状态

```powershell
thatperson status        # 状态卡片：版本 / 模型 / 记忆条目 / 技能数量 / Token 预算 / 目录
```

正常应显示：版本 1.2.0、技能数量 5（code-op / industry-analysis / prompt-op / vault-api-bridge / warehouses-management）。

## 三、首次配置（API Key / 模型）

### 3.1 配置 API Key

优先使用配置向导（掩码输入、不回显、不落日志）：

```powershell
thatperson setup
```

向导会引导你输入 API Key 与模型，写回 `~/.thatperson/config.json`（`apiKey` 掩码存储、`configured` 标记置 true）。

也可直接配置：

```powershell
thatperson config set apiKey sk-你的Key     # Key 掩码存储于 config.json
```

或设置环境变量：`$env:AAGENTDS_API_KEY='sk-你的Key'`（优先级最高：环境变量 > config.json.apiKey）。

安全约定：CLI 不硬编码、不打印 Key；`.env` 与 `API-key.md` 均被 .gitignore 排除。

### 3.2 查看 / 修改模型

```powershell
thatperson config get model          # 查看当前模型
thatperson config set model deepseek-v4-flash   # 修改模型
```

默认模型为 `deepseek-v4-flash`（config.json 唯一来源）。

## 四、快速上手

```powershell
thatperson                  # 进入持续对话模式
thatperson --mock           # 离线演示（不调用 API、不发网络，可无 Key 试用）
thatperson "你好，介绍一下你自己"   # 一次性提问（位置参数）
thatperson --input-file input.txt   # 从文件读入指令，单次对话后退出
```

对话内输入 `exit` / `quit` / `退出` / `再见` 结束。

## 五、命令行参考

### 5.1 全局参数

| 参数 | 作用 |
| :--- | :--- |
| `--version` / `-V` | 输出版本号后退出（任意目录均显示本包版本） |
| `--help` / `-h` | 打印帮助后退出 |
| `--mock` | 离线演示模式（不读 Key、不发网络） |
| `--input-file <path>` | 从 UTF-8 文件读入指令，单次对话后退出 |

### 5.2 全局子命令

| 命令 | 作用 |
| :--- | :--- |
| `status` | 系统状态卡片（版本/模型/记忆/技能/预算/目录） |
| `setup` | 首次配置向导（API Key / 模型，写回 config.json） |
| `wizard` | setup 的别名 |
| `reset` | 重置配置（仅保留 apiKey 与 model；`--keep-present` 保留 present 覆盖） |
| `present init` | 生成出厂人格模板到主目录 present/（不覆盖既有文件） |
| `present show` | 查看当前生效人格 |
| `tools list` | 列出已注册工具（read/write/danger） |
| `update` | 手动检查更新（绕过 12h 缓存；本地开发路径仍跳过） |
| `memory search <关键词>` | 在记忆中搜索关键词 |
| `memory stats` | 记忆统计（分 section 条目数） |
| `memory clean` | 对归档文件执行压缩清理 |
| `session list` | 列出历史会话 |
| `session clear` | 清空当前内存会话 |
| `config get [key]` | 查看配置（model / disabledSkills / apiKey，apiKey 掩码回显） |
| `config set <key> <val>` | 修改配置（apiKey 掩码回显） |
| `skills list` | 列出已安装技能与启用状态 |
| `skills enable|disable <名称>` | 启用/禁用技能 |
| `help` | 打印帮助 |

### 5.3 对话内内部指令

`/help` `/history` `/clear` `/reset` `/exit` `/save` `/update`

- `/save`：将当前会话落盘到 `history/sessions/`（不覆盖同名文件）。
- 技能调用：输入 `/<技能名>`（如 `/code-op`）或说出触发词自动匹配。

## 六、记忆与配置目录

| 目录 | 作用 |
| :--- | :--- |
| `~/.thatperson/config.json` | 配置（模型、禁用技能、`apiKey` 掩码存储、`configured` 标记），首次自动生成 |
| `~/.thatperson/present/` | 用户级人格基线 |
| `~/.thatperson/skills/` | 用户级技能（可扩展） |
| `~/.thatperson/history/` | 全局部署模式记忆 |
| `~/.thatperson/logs/` | 运行日志 |
| `<cwd>/.thatperson/`（随身目录） | 存在时承载随身记忆（`history/`）与人格/技能覆盖 |

- `THATPERSON_HOME`：自定义全局目录（默认 `~/.thatperson/`）。
- `THATPERSON_MEMORY_DIR`：显式指定记忆目录（优先级最高）。
- 随身目录：存在 `<cwd>/.thatperson/` 时，记忆落 `<cwd>/.thatperson/history/`；否则落主目录 `~/.thatperson/history/`。
- 旧配置文件（无 `apiKey`/`configured` 字段）读取时给缺省值，不静默改写文件。

## 七、人格与技能加载优先级

- 人格（Present）：**主目录 `~/.thatperson/present/` > 随身目录 `<cwd>/.thatperson/present/` > 出厂级（包内 `present/`）**。缺失文件自动回退下一级，同名不覆盖。
- 技能：出厂技能库随包提供（5 个）；用户级 `~/.thatperson/skills/` 可扩展；`skills disable` 可关闭。

## 八、常见问题（FAQ）

1. **`npm` 报「禁止运行脚本」**：用 `npm.cmd`；或 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`。
2. **`thatperson` 提示无法加载 .ps1**：用 `thatperson.cmd` 或放开执行策略（见 2.2）。
3. **对话报无 Key**：运行 `thatperson setup` 完成首次配置，或用 `thatperson config set apiKey <Key>` / 设置环境变量 `AAGENTDS_API_KEY`；`thatperson status` 不显示 Key（掩码存储，安全设计）。
4. **如何验证出厂人格生效**：隔离环境（`$env:THATPERSON_HOME=$env:TEMP\tp-home`）在任意空目录运行 `thatperson --mock`，回复应带 ThatPerson 人格（出厂 identity/行为准则/能力清单/输出习惯/风格 5 维兜底）。
5. **更新检查 404 静默**：本地开发路径（`G:\XXFS\`）自动跳过更新检查，属正常设计。

## 九、更新、回退与卸载

```powershell
npm.cmd update -g @nineteenfolk/thatperson    # 更新到最新版
npm.cmd install -g @nineteenfolk/thatperson@1.2.0   # 回退到指定版本
npm.cmd uninstall -g @nineteenfolk/thatperson # 卸载
```

> 卸载不影响 `~/.thatperson/` 记忆数据（如需彻底清除请手动删除该目录）。

## 十、版本与发布规范（Semver）

每次发布遵循语义化版本，用 `npm version` 自动升级并打 tag：

| 命令 | 场景 | 示例 |
| :--- | :--- | :--- |
| `npm version patch` | 修复 bug | 1.0.0 → 1.0.1 |
| `npm version minor` | 新增功能 | 1.0.0 → 1.1.0 |
| `npm version major` | 破坏性更新 | 1.0.0 → 2.0.0 |

发布前 `prepublishOnly` 自动执行构建与全量测试（156 用例：155 通过 / 1 跳过 POSIX 0600），发布内容仅 `dist / present / skills`（files 白名单）。
