# ThatPerson 项目目录树

> 项目名称：ThatPerson（原 ThatGirl）
> 定位：无限接近人的**个人管家** CLI（对话式 + 长期记忆 + 技能系统 + 出厂人格）
> 技术栈：TypeScript + Node.js 24，核心逻辑零依赖（工具层 node:fs / node:path / node:child_process 原生实现；运行时仅 7 个表现层/解析库：6 UI + commander）
> 生成日期：2026-08-22（第 5 期同步：批次一归档三闸/部署体验/定位去比喻 + 批次二工具层/ReAct/安全收口）

## 一、目录树总览

```text
ThatPerson/
├── .claude/                        # 项目级 Claude Skills（供 Claude Code 使用，gitignore）
│   └── skills/                     # code-op / industry-analysis / prompt-op / vault-api-bridge / warehouses-management
├── .codex/                         # 项目级 Codex Skills（与 .claude/skills 镜像，gitignore）
│   └── skills/                     # 同上五件套
├── .env                            # 环境变量（AAGENTDS_API_KEY，gitignore）
├── .git/                           # Git 版本库
├── .gitignore                      # 忽略规则：密钥 / 构建产物 / IDE / 智能体配置 / history/（第 5 期新增）
├── .idea/                          # JetBrains IDE 本地配置
├── API-key.md                      # API Key 使用说明（明文副本，gitignore）
├── package.json                    # npm 项目清单：name=@nineteenfolk/thatperson（scoped）/ version 1.2.0 / bin → dist/src/cli.js
├── package-lock.json               # 依赖锁文件
├── tsconfig.json                   # TypeScript 基础配置（IDE / 类型检查）
├── tsconfig.build.json             # 主程序构建配置（仅 src → dist/）
├── tsconfig.test.json              # 测试编译配置（src + tests → dist-test/）
├── ARCHITECTURE.md                 # 架构控制文档（活地图，第 5 期同步 src/tools/ + loop + 常量）
├── README.md                       # 项目说明（个人管家定位，第 5 期更新）
├── 使用说明.md                      # 从部署到使用（第 5 期更新：setup / 目录表 / 命令参考）
├── 目录树.md                        # 本文件（第 5 期重新生成）
├── 需要持续更新的文件.txt            # 需随迭代同步的文件清单
│
├── src/                            # 主程序源码（TypeScript）
│   ├── chat.ts                     # 共享对话引擎：四层注入 + 技能摘要层 + 工具清单层 / 检索（段落化）/ Function Calling（tools+tool_choice）/ ChatResult{toolCalls}
│   ├── cli.ts                      # 持续对话 CLI + 全局命令（parseArgs / 内部指令 / 全局子命令含 setup/present/tools / 内容模式 / runAgentLoop 接线）
│   ├── config.ts                   # 全局配置：resolveApiKey（环境变量>config.json>.env）/ maskApiKey / isConfigured / resetConfig / model 唯一来源 / 随身目录模型
│   ├── setup.ts                    # 配置向导 runSetupWizard（第 5 期新增：password 掩码输入 Key，不打印不落日志）
│   ├── index.ts                    # 单次命令入口（npm run dev / mock）
│   ├── present.ts                  # Present 元认知：主目录→随身目录→出厂级联 / presentInit / presentShowText
│   ├── report.ts                   # 项目报告自动生成器
│   ├── skill.ts                    # Skill 调用系统（主目录→随身→包内级联扫描 / trigger_keywords+tools 声明 / 路径白名单）
│   ├── agent/
│   │   └── loop.ts                 # ReAct 循环（第 5 期新增）：解析→执行→回灌→再推理；MAX_TOOL_ITERATIONS=12（可调）；审计日志 argsKeys
│   ├── memory/
│   │   ├── store.ts                # 记忆存储实现（归档 / 转义 / 压缩 / 上限）
│   │   └── types.ts                # 记忆共享契约（类型 + 目录文件映射，第 5 期加 journal）
│   ├── parser/
│   │   ├── archive.ts              # 对话归档解析器（三闸：单句单条目/占位词黑名单/不确定不产经历 + 内容模式 extractContentModeArchives）
│   │   └── llm-archive.ts          # LLM 语义归档（增强层默认关闭；独立 AAGENTDS_ARCHIVE_API_KEY；第 5 期 M2 增强）
│   ├── tools/                      # 工具层（第 5 期新增）
│   │   ├── types.ts                # 工具契约（ToolDef / ToolParams / ToolContext / ToolResult）
│   │   ├── registry.ts             # 工具注册表白名单（getTool / listTools / buildToolSpecs）
│   │   ├── guards.ts               # 守卫（validateParams / assertPathAllowed realpath 复检 / truncateResult 16000）
│   │   ├── executor.ts             # 执行器（unknown-tool / danger-disabled / 参数校验 / 截断 / 异常捕获）
│   │   └── builtin.ts              # 内置 8 工具（read×5 + write×2 + run_shell 门控）
│   └── utils/
│       ├── ui.ts                   # CLI 表现层 UI（logger / showBanner / showStatusCard / startSpinner / ask）
│       └── update-check.ts         # 更新检查（REGISTRY_URL 已对齐 scoped 包名 @nineteenfolk%2fthatperson）
│
├── tests/                          # 测试套件（node:test，全离线，156 用例）
│   ├── helpers.ts                  # 测试隔离工具（临时 home / 目录快照守卫）
│   ├── isolation.test.ts           # IS-1~IS-3：测试与主程序隔离守卫（3）
│   ├── security.test.ts            # SEC-1~12 + 掩码/向导卫生/桥接（18，第 5 期加 SEC-10/11/12）
│   ├── fuzz.test.ts                # FZ-1~FZ-5：17 个载荷模糊变体（6）
│   ├── badcases.test.ts            # BC-1~BC-12（12，第 5 期加 BC-10/11/12）
│   ├── chat.test.ts                # Present / System 组装 / 检索 / 技能摘要层 / 先回应内容（12）
│   ├── config.test.ts              # 目录定位 / resolveApiKey / 掩码 / 0600 / reset（16）
│   ├── parser.test.ts              # 归档解析器 + 占位词/长文本（16）
│   ├── store.test.ts               # 记忆存储测试（6）
│   ├── cli.test.ts                 # CLI 参数 / 内部指令 / 全局指令（present/tools/setup）/ 向导不弹（30）
│   ├── update-check.test.ts        # 12h 缓存 / force / 404 静默 / scoped URL（20）
│   └── tools.test.ts               # 工具层：注册表 / 门控 / 参数校验 / 路径穿越 / 截断 / loop 3 路径（17，第 5 期新增）
│
├── skills/                         # 出厂技能库（随 npm 打包，用户级同名可覆盖）
│   ├── README.md                   # 技能库说明与目录约定
│   ├── code-op/                    # 代码优化技能（SKILL.md + examples / references / templates）
│   ├── industry-analysis/          # 行业分析技能
│   ├── prompt-op/                  # 提示词优化技能
│   ├── vault-api-bridge/           # Obsidian 知识库桥接技能（frontmatter 声明 tools: read_vault_note/search_vault/search_memory，第 5 期）
│   └── warehouses-management/      # 知识库整理技能
│
├── present/                        # 出厂人格（第 5 期中性化「个人管家」，主目录→随身→出厂级联）
│   ├── behavior.md                 # 行为准则（含能力自查开关条目）
│   ├── capabilities.md             # 能力清单（技能 / CLI / 记忆 / 边界）
│   ├── identity.md                 # 身份声明（我是谁）
│   ├── output.md                   # 输出习惯
│   └── persona.md                  # 人格设定
│
├── history/                        # 项目本地记忆（第 5 期起 gitignore，记忆统一落主目录 ~/.thatperson/history/）
│   ├── README.md
│   ├── profile/                    # identity / preferences / traits
│   ├── timeline/                   # milestones / important_dates
│   ├── experiences/                # journal.md
│   ├── insights/                   # patterns.md
│   ├── sessions/                   # /save 会话快照
│   └── session_logs/               # 每日对话摘要
│
├── ThatPerson反馈收集/             # 用户反馈
│   ├── 文件夹说明.md               # 反馈目录约定
│   ├── Open/                       # 待处理反馈（文件夹说明.md + 运行时路径授权需求-20260822）
│   └── Done/                       # 已解决反馈归档（第 5 期新增 4 份：架构优化方案/第四期问题核查报告/解决方案与发展方向规划/全局部署首次引导，后缀 -已处理20260822）
│
├── ThatPerson项目资源文件/         # 项目知识库 / 资源
│   ├── Agent 项目.md               # 项目总览
│   ├── 可用模板/                   # 项目报告提交模板 / Git提交说明模板 / 安全审查报告模板 / 项目版本规范
│   ├── 提示词文件夹/               # 第一~五版提示词（第五版 v2.1 为第 5 期执行任务书）
│   ├── 行业数据/                   # AI智能体 / IT计算机 / 单身经济 行业分析报告
│   ├── 领域相关资料/               # Agent 上下文工程 / AI Agent 记忆机制 / Skills 教程 / ReAct 模式 / Tool Use 模式 等方法论
│   ├── 参考代码/                   # 参考实现（Open/Done 分类）
│   ├── 安全专项/                   # 测试地图 / 安全测试工具操作指南（第 5 期同步 SEC-10~12）/ 第三期/ / 第四期/ / 第五期（安全沙箱设定说明）
│   └── CLI生态相关/
│       ├── CLI生态说明清单.md            # CLI 生态说明清单（第 4 期建立，第 5 期更新至 v1.2.0 指令全集）
│       └── CLI界面优化相关/        # CLI界面优化报告.md（第 4 期交付物）
│           （第 1 期 CLI 参考文档：CLI生态/更新检查/CLI美化 三篇已移入 参考代码\Done\）
│
├── 项目报告/                      # 项目报告（按期次归档）
│   ├── 第一期/                    # 第 1 期三件套
│   ├── 第二期/                    # 第 2 期三件套
│   ├── 第三期/                    # 第 3 期三件套 + KeySpecs + 迭代总结 + 安全测试专项
│   ├── 第四期/                    # 第 4 期三件套 + CLI生态说明清单 + CLI界面优化报告
│   └── 第五期/                    # 第 5 期 KeySpecs + 反馈转任务清单 + 三件套（状态/安全/Git说明）
│
├── dist/                          # 主程序构建产物（gitignore，tsc -p tsconfig.build.json）
│   └── src/                       # 主程序编译输出（含 tools/、agent/loop.js）
├── dist-test/                     # 测试编译产物（gitignore，tsc -p tsconfig.test.json）
└── node_modules/                  # npm 依赖（gitignore：devDependencies + 7 个运行时依赖）
```

## 二、分项详细说明

### 1. 根目录配置
- `package.json`：项目清单。`name=@nineteenfolk/thatperson`（scoped，第 4 期发布前置，第 5 期 REGISTRY_URL 对齐）；`version=1.2.0`；`bin` 注册全局 `thatperson` → `./dist/src/cli.js`；`scripts` 提供 `build / dev / start / mock / chat / chat:mock / test / report`；`prepublishOnly` = build + test；`files` 含 `dist` + `present` + `skills`；`dependencies` 含 7 个运行时依赖（6 UI + commander，供应链评审通过）。
- `tsconfig.build.json`：仅编译 `src/` → `dist/`（含 `src/tools/`、`src/agent/`）。
- `tsconfig.test.json`：编译 `src/` + `tests/` → `dist-test/`（测试与主程序隔离）。
- `.gitignore`：密钥（.env / API-key.* / *.key）、构建产物（dist/ dist-test/）、IDE（.idea/）、智能体配置（.claude/ .codex/ .agents/）、`history/`（第 5 期新增：项目本地记忆不入库）、`项目报告/*/目录树.md`（可再生成）。
- `ARCHITECTURE.md`：架构活地图，第 5 期已同步（数据流含 ReAct/工具链、文件地图含 src/tools/ 与 src/agent/loop.ts、常量 MAX_TOOL_ITERATIONS 等、CR-023~CR-032、遗留 P1~P8）。

### 2. `src/` 主程序源码
- `chat.ts`：共享对话引擎。第 5 期变更：回复指令「先回应内容再回应动作」；检索语料段落化 + journal 语料；`ChatResult { content, toolCalls? }`；`ChatMessage` 支持 role='tool'；请求体 tools+tool_choice:'auto'；`buildToolSummary` 生成 `<工具清单>`（SEC-10 静态不可注入）；Key 改走 `resolveApiKey()`。
- `cli.ts`：第 5 期变更：`setup/wizard/reset/present init|show/tools list` 全局指令；无 Key 自动弹向导（TTY 且未 configured）；`config get/set apiKey` 掩码回显；长文本内容模式（>200 字全文归档）；普通消息走 `runAgentLoop`（自动注册 builtins）。
- `config.ts`：第 5 期变更：`apiKey?/configured?` 字段；`CONFIG_KEY_WHITELIST` 增 apiKey；`resolveApiKey()` 三来源；`maskApiKey()` 掩码；`resetConfig()` 仅保留 model+apiKey；`localThatPersonDir()` 随身目录模型；ensureConfigDir 补 history/ 且 0600 写盘。
- `setup.ts`（第 5 期新增）：`runSetupWizard()`——inquirer password 输入 Key（掩码、不打印、不落日志），写回 config.json（保留既有字段），非交互入口不弹。
- `agent/loop.ts`（第 5 期新增）：ReAct 循环——解析（chat toolCalls）→ 执行（executeTool）→ 回灌（role='tool'）→ 再推理；`MAX_TOOL_ITERATIONS=12` 硬上限（`THATPERSON_MAX_TOOL_ITERATIONS` 可调）；连续失败 3 次认输；审计日志 `logs/tool-*.jsonl` 只记 argsKeys；`--mock` 走 `THATPERSON_MOCK_TOOL_CALLS` 注入。
- `tools/`（第 5 期新增）：`types.ts` 契约 → `registry.ts` 注册表白名单 → `guards.ts` 守卫（路径白名单 + realpath 复检 / 参数校验 / 结果截断 16000，`THATPERSON_RESULT_CHAR_LIMIT` 可调）→ `executor.ts` 统一执行入口 → `builtin.ts` 内置 8 工具（list_directory/read_file/read_vault_note/search_vault/search_memory/append_memory/edit_present/run_shell；run_shell 需 `THATPERSON_ENABLE_SHELL=true` 才注册且双门控）。
- `parser/archive.ts`：第 5 期三闸——单句单条目（句子+极性去重）、占位词黑名单（事情/东西/感觉等）、不确定不产经历；新增 `extractContentModeArchives`（长文本内容通道）。
- `parser/llm-archive.ts`：第 5 期 M2 增强——独立 Key `AAGENTDS_ARCHIVE_API_KEY`（禁止复用主 Key）、insight 语义概括（禁止截取原话）、同条去重、不确定时 confidence=中且不同时产偏好+经历、schema 校验防伪造；`--mock` 返回 null 走规则版兜底。

### 3. `tests/` 测试套件（第 5 期全量 156 用例：155 通过 / 1 跳过 POSIX 0600 / 0 失败）
- `tools.test.ts`（第 5 期新增，17 用例）：注册表白名单（≥5 read/write）/ run_shell 环境变量门控 / buildToolSpecs 静态精简 / 未注册拒绝 / danger 双门控 / 参数校验 / 路径穿越（../、白名单外、符号链接）/ 结果截断 / read_vault_note / search_vault / append_memory / edit_present / loop 三段 / 12 轮上限 / 连续失败认输 / 失败重试成功。
- `security.test.ts`（18 用例）：SEC-1~9（第 4 期口径）+ SEC-10（工具清单静态不可注入）/ SEC-11（tool_result 边界闭合）/ SEC-12（run_shell 双门控）+ 掩码回显 / 向导卫生 / KS-21 桥接。
- `badcases.test.ts`（12 用例）：BC-1~9 + BC-10（同句多锚点单条目）/ BC-11（不确定不产经历）/ BC-12（长文本内容归档）。
- `config.test.ts`（16 用例）：目录定位 / resolveApiKey 优先级 / 掩码 / config.json 0600 / configured / reset / 向导不弹分支。
- `cli.test.ts`（30 用例）：--version 目录生成 / setup/wizard/present/tools 指令 / status 掩码 Key 等。
- `chat.test.ts`（12 用例）：含「先回应内容」回复指令断言。
- `parser.test.ts`（16 用例）：含占位词黑名单与长文本归档用例。

### 4. `present/` 出厂人格
- `identity.md`：身份声明（我是谁），第 5 期中性化「个人管家」，去「=大脑/=手/=记忆」比喻。
- `persona.md`：性格与语气设定（温暖、细腻、善于倾听的个人管家）。
- `behavior.md`：行为准则（回复只融入 ≤1 条相关记忆、不机械罗列、不主动打探隐私；含能力自查开关）。
- `output.md`：输出习惯与格式偏好。
- `capabilities.md`：能力清单（技能 5 个 / CLI 指令 / 记忆能力 / 能力边界），经 `loadPresent()` 自动注入 System。
- 加载级联：主目录 `~/.thatperson/present/` > 随身目录 `<cwd>/.thatperson/present/` > 出厂 `present/`（缺失文件自动回退下一级，同名不覆盖）；`present init` 生成模板、`present show` 查看生效人格。

### 5. `history/` 项目本地记忆（第 5 期起 gitignore）
- 第 5 期起记忆统一落主目录 `~/.thatperson/history/`（随身目录 `<cwd>/.thatperson/history/` 优先），项目 `history/` 不再入库。
- 结构：`profile/`（identity/preferences/traits）、`timeline/`（milestones/important_dates）、`experiences/journal.md`、`insights/patterns.md`、`sessions/`（/save 快照）、`session_logs/`（每日摘要）。

### 6. `ThatPerson反馈收集/`
- `Open/`：待处理反馈（`文件夹说明.md` + `ThatPerson运行时路径授权需求-20260822.md`——allow-dir 动态授权，建议第 6 期排期）。
- `Done/`：第 5 期新增 4 份已处理反馈（后缀 `-已处理20260822`）：架构优化方案 / 第四期问题核查报告 / 解决方案与发展方向规划 / 全局部署首次引导问题分析与改进方案。

### 7. `ThatPerson项目资源文件/`（项目知识库）
- `提示词文件夹/`：第一~五版提示词（第五版 v2.1 为第 5 期执行任务书）。
- `可用模板/`：项目报告提交模板 / Git提交说明模板 / 安全审查报告模板 / 项目版本规范。
- `领域相关资料/`：Agent 设计模式（ReAct 深度解析）/ Tool Use 模式 / Skills 教程 / AI Agent 记忆机制 / 上下文工程 等方法论（第 5 期新增 ReAct 与 Tool Use 资料）。
- `安全专项/`：`测试地图.md` 与 `安全测试工具操作指南.md`（第 5 期同步 SEC-10~12 / tools.test.ts / BC-10~12 / 工具层红队）、`第三期/`、`第四期/`、`LLM红队报告-20260812.md`、`scripts/`（redteam-run.js）。
- `参考代码/`：参考实现（Open/Done 分类，未跟踪资源）。

### 8. `项目报告/`
- `第五期/`（第 5 期新增）：`第5期-KeySpecs-20260822.md`（24 条硬规格）+ `第5期-反馈转任务清单-20260822.md`（46 条任务）+ 三件套：`ThatPerson项目状态报告-第5期-20260822.md` / `ThatPerson安全审查-第5期-20260822.md` / `Git提交说明-第5期-20260822.md`。
- `目录树.md`：本文件，项目结构全景说明（gitignore，可再生成）。

### 9. `skills/` 出厂技能库（随 npm 打包）
- `vault-api-bridge/`：Obsidian 知识库桥接技能——第 5 期 SKILL.md frontmatter 新增 `tools: [read_vault_note, search_vault, search_memory]` 声明，触发技能时动态注册进本轮工具列表（技能 = 工具组合说明书）。
- 其余技能（code-op / industry-analysis / prompt-op / warehouses-management）目录结构不变。
