# ThatPerson 项目目录树

> 项目名称：ThatPerson（原 ThatGirl）
> 架构：API=大脑 / Skill=手 / Markdown=记忆
> 技术栈：TypeScript + Node.js 24，核心逻辑零依赖（运行时仅 7 个表现层/解析库：6 UI + commander）
> 生成日期：2026-08-12（第 4 期同步）

## 一、目录树总览

```text
ThatPerson/
├── .claude/                        # 项目级 Claude Skills（供 Claude Code 使用，gitignore）
│   └── skills/                     # code-op / industry-analysis / prompt-op / vault-api-bridge / warehouses-management
├── .codex/                         # 项目级 Codex Skills（与 .claude/skills 镜像，gitignore）
│   └── skills/                     # 同上五件套
├── .env                            # 环境变量（AAGENTDS_API_KEY，gitignore）
├── .git/                           # Git 版本库
├── .gitignore                      # 忽略规则：密钥 / 构建产物 / IDE / 智能体配置
├── .idea/                          # JetBrains IDE 本地配置
├── API-key.md                      # API Key 使用说明（明文副本，gitignore）
├── package.json                    # npm 项目清单：脚本 / 入口 / bin（name=thatperson / version 1.1.0）
├── package-lock.json               # 依赖锁文件
├── tsconfig.json                   # TypeScript 基础配置（IDE / 类型检查）
├── tsconfig.build.json             # 主程序构建配置（仅 src → dist/）
├── tsconfig.test.json              # 测试编译配置（src + tests → dist-test/）
│
├── src/                            # 主程序源码（TypeScript）
│   ├── chat.ts                     # 共享对话引擎：System 组装 / 四层记忆 + 技能摘要层 / 检索 / 调 API
│   ├── cli.ts                      # 持续对话 CLI + 全局命令（parseArgs / 内部指令 / 全局子命令 / 工具通道）
│   ├── config.ts                   # 全局配置：model 唯一来源 / config get-set / disabledSkills / api-key 引导
│   ├── index.ts                    # 单次命令入口（npm run dev / mock）
│   ├── present.ts                  # Present 元认知加载与边界拼接（自动加载 capabilities.md）
│   ├── report.ts                   # 项目报告自动生成器
│   ├── skill.ts                    # Skill 调用系统（扫描 / 匹配 / YAML trigger_keywords / 路径白名单）
│   ├── memory/
│   │   ├── store.ts                # 记忆存储实现（归档 / 转义 / 压缩 / 上限）
│   │   └── types.ts                # 记忆共享契约（类型 + 目录文件映射）
│   ├── parser/
│   │   ├── archive.ts              # 对话归档解析器（提炼记忆 / 摘要 / 跨轮模式 / P0 极性修复）
│   │   └── llm-archive.ts          # LLM 语义归档（增强层，默认关闭，第 4 期新增）
│   └── utils/
│       ├── ui.ts                   # CLI 表现层 UI（logger / showBanner / showStatusCard / startSpinner / ask，第 4 期新增）
│       └── update-check.ts         # 更新自动检查（12h 缓存 / 跳过策略 / 静默失败，第 4 期新增）
│
├── tests/                          # 测试套件（node:test，全离线）
│   ├── helpers.ts                  # 测试隔离工具（临时 home / 目录快照守卫）
│   ├── isolation.test.ts           # IS-1~IS-3：测试与主程序隔离守卫
│   ├── security.test.ts            # SEC-1~SEC-9：提示词注入等安全回归（SEC-6 第 4 期口径）
│   ├── fuzz.test.ts                # FZ-1~FZ-5：17 个载荷模糊变体（第 4 期新增）
│   ├── badcases.test.ts            # BC-1~BC-9：第 3/4 期验收回归（含归档极性修复）
│   ├── chat.test.ts                # Present / System 组装 / 检索 / 技能摘要层测试
│   ├── config.test.ts              # 目录定位 / 项目模式 / 配置读写测试
│   ├── parser.test.ts              # 归档解析器测试
│   ├── store.test.ts               # 记忆存储测试
│   ├── cli.test.ts                 # CLI 参数解析 / 内部指令表 / 全局指令 / 工具通道（第 4 期新增）
│   └── update-check.test.ts        # 12h 缓存 / force / 404 静默 / 版本对比（第 4 期新增）
│
├── skills/                         # 出厂技能库（随 npm 打包，用户级同名可覆盖）
│   ├── README.md                   # 技能库说明与目录约定
│   ├── code-op/                    # 代码优化技能（SKILL.md + examples / references / templates）
│   ├── industry-analysis/          # 行业分析技能
│   ├── prompt-op/                  # 提示词优化技能
│   ├── vault-api-bridge/           # Obsidian 知识库桥接技能（含 scripts/自动化脚本/）
│   └── warehouses-management/      # 知识库整理技能
│
├── present/                        # Present 元认知（出厂人格，项目级可覆盖用户级）
│   ├── behavior.md                 # 行为准则（第 4 期新增能力自查开关条目）
│   ├── capabilities.md             # 能力清单（技能 / CLI / 记忆 / 边界，第 4 期新增）
│   ├── identity.md                 # 身份声明（我是谁）
│   ├── output.md                   # 输出习惯
│   └── persona.md                  # 人格设定
│
├── history/                        # 长期记忆（项目模式记忆根，随仓库演进）
│   ├── README.md                   # 记忆目录说明与数据卫生约定
│   ├── profile/                    # 身份画像：identity / preferences / traits（按需生成）
│   ├── timeline/                   # 人生节点：milestones / important_dates
│   ├── experiences/                # 经历日志：journal.md
│   ├── insights/                   # 模式洞察：patterns.md
│   ├── sessions/                   # /save 会话快照：session-<时间戳>.md（第 4 期新增）
│   └── session_logs/               # 每日对话摘要：YYYY-MM-DD.md
│
├── ThatPerson反馈收集/             # 用户反馈
│   ├── 文件夹说明.md               # 反馈目录约定
│   ├── Open/                       # 待处理反馈（当前仅文件夹说明.md）
│   └── Done/                       # 已解决反馈归档（含第三期两份反馈 + 第 4 期处理状态）
│
├── ThatPerson项目资源文件/         # 项目知识库 / 资源
│   ├── 第4期-KeySpecs-20260811.md  # 第 4 期硬性技术规格（R-1 交付物）
│   ├── Agent 项目.md               # 项目总览
│   ├── 可用模板/                   # 项目报告提交模板 / Git提交说明模板 / 安全审查报告模板
│   ├── 提示词文件夹/               # 第一/二/三/四版提示词（第四版为本期执行任务书）
│   ├── 行业数据/                   # AI智能体 / IT计算机 / 单身经济 行业分析报告
│   ├── 领域相关资料/               # Agent 上下文工程 / AI Agent 记忆机制 / Skills 教程 等方法论
│   ├── 安全专项/                   # 安全测试环境搭建指南 / 测试地图 / 安全测试工具操作指南 / 文件夹说明
│   └── CLI生态相关/
│       ├── 指令相关/               # 关于ThatPerson完整的CLI生态开发第一期.md（参考）
│       ├── 更新检查/               # 关于ThatPerson完整的更新检查开发第一期.md（参考）
│       ├── CLI美化/                # 关于ThatPerson完整的CLI美化开发第一期.md（参考）
│       ├── 说明文档/               # CLI生态说明清单.md（第 4 期交付物）
│       └── CLI界面优化相关/        # CLI界面优化报告.md（第 4 期交付物）
│
├── 项目报告/                      # 项目报告（按期次归档）
│   ├── 目录树.md                  # 本文件
│   ├── 第一期/                    # 第 1 期三件套
│   ├── 第二期/                    # 第 2 期三件套
│   ├── 第三期/                    # 第 3 期三件套 + KeySpecs + 迭代总结 + 安全测试专项
│   └── 第四期/                    # 第 4 期三件套 + CLI生态说明清单 + CLI界面优化报告（双落点副本）
│
├── dist/                          # 主程序构建产物（gitignore，tsc -p tsconfig.build.json）
│   ├── index.js                   # 历史遗留旧产物（顶层入口）
│   └── src/                       # 主程序编译输出（index.js / cli.js / chat.js / utils/ 等）
├── dist-test/                     # 测试编译产物（gitignore，tsc -p tsconfig.test.json）
│   └── tests/                     # 测试编译输出（*.test.js + helpers.js）
└── node_modules/                  # npm 依赖（gitignore：devDependencies + 7 个运行时依赖）
```

## 二、分项详细说明

### 1. 根目录配置
- `package.json`：项目清单。`name=thatperson`（小写，第 4 期发布前置）；`version=1.1.0`；`bin` 注册全局 `thatperson` → `./dist/src/cli.js`；`scripts` 提供 `build / dev / start / mock / chat / chat:mock / test / report`；`files` 含 `dist` + `skills`；`dependencies` 含 7 个运行时依赖（6 UI + commander，第 4 期供应链评审通过）。
- `package-lock.json`：锁定 devDependencies 与 dependencies 版本，保证环境可复现。
- `tsconfig.json`：TypeScript 基础配置（target es2016 / commonjs / strict / rootDir `.` / outDir `dist` / include `src+tests`），供 IDE 与全局类型检查。
- `tsconfig.build.json`：主程序构建配置，继承基础配置但 `include` 仅 `src`，输出 `dist/`——保证主程序产物不混入测试。
- `tsconfig.test.json`：测试编译配置，`include src+tests`，输出独立 `dist-test/`——实现测试与主程序编译产物隔离。
- `.gitignore`：忽略 `.env`、`API-key*.md`、`dist/`、`dist-test/`、`node_modules/`、`.idea/`、`.claude/`、`.codex/`、`.agents/` 等，防止密钥与产物入库。
- `.env`：环境变量文件，存放 `AAGENTDS_API_KEY`（DeepSeek Key），仅本机存在，绝不提交。
- `API-key.md`：API Key 获取/使用/轮换说明（含明文副本，已被 gitignore，建议轮换后改占位符）。
- `.idea/`：JetBrains（WebStorm）本地 IDE 配置，不影响项目运行。

### 2. `src/` 主程序源码
- `chat.ts`：对话引擎核心。`loadEnv` 读取 `.env`；`BASE_URL` 白名单常量；`config.model` 唯一模型来源（第 4 期 CR-018）；`estimateTokens`/`foldSummary` 预算与摘要折叠（含输入侧转义 FZ-4b）；`buildSystemPrompt` 四层记忆回灌 + `<技能清单>` 摘要层 + `<临近提醒>`；`buildSkillsSummary` 技能摘要；`retrieveRelevant` 标签倒排检索；`chat()` 组装消息调用 DeepSeek，`--mock` 模式离线返回，错误体 `sk-***` 脱敏。
- `cli.ts`：持续对话 CLI + 全局命令（第 4 期重写）。`parseArgs` 全局参数解析（--version/-V/--help/-h/--mock/--input-file）；`createInternalCommands` 内部指令表（/help /history /clear /reset /exit /save /update）；`runGlobalCommand` 全局子命令（status/update/help/memory/session/config/skills）；`detectToolIntent`/`runTool` 指令-执行-返回通道；`processInput` 优先级（内部指令 > Skill 斜杠 > 工具通道 > 自然语言）；LLM 语义归档动态接线；启动异步更新检查。
- `config.ts`：目录定位中枢。`THATPERSON_HOME` 解析、`ensureConfigDir`、`isProjectMode`/`memoryRoot`/`resolveHistoryDir` 三档定位；第 4 期新增 `DEFAULT_MODEL`、`CONFIG_KEY_WHITELIST`、`getConfigValue`/`setConfigValue`、`disableSkill`/`enableSkill`/`listDisabledSkills`、`apiKeyGuidance`/`hasApiKey`。
- `index.ts`：单次命令入口（`npm run dev/mock <问题>`），一次完成「对话 → 归档 → 写当日摘要」。
- `present.ts`：Present 元认知加载——用户级 `~/.thatperson/present/` 为全局基线，项目 `present/` 同名覆盖；自动加载 `capabilities.md`（第 4 期）；`buildPresentBlock` 用 `<present>` 边界拼接，防注入。
- `report.ts`：项目报告生成器。读取模板，扫描资源文档与已有报告，自动检测期次并生成 `第N期` 状态报告。
- `skill.ts`：Skill 系统。解析 SKILL.md frontmatter（name/description/trigger_keywords，第 4 期修复多行 YAML 列表解析 CR-021）；扫描用户级 + 项目级 + 包内 `skills/`；`/名称` 斜杠触发 + 关键词自动触发；`listSkills` 过滤 disabledSkills（第 4 期）；`loadSkill` 路径白名单守卫（拒绝 `..` 与分隔符穿越）。
- `memory/store.ts`：记忆存储。五维目录结构、归档追加（同日期标题合并）、`sanitizeForMarkdown` 转义 `<`/`>`/换行防标签闭合、压缩机制（跨会话去重、低置信度 30 天衰减、标签折叠、每文件软上限 100 条）。
- `memory/types.ts`：记忆共享契约（只定义不改）。`MemorySection`、`ArchiveType`、`Confidence`、`ArchiveEntry`、`SessionSummary`、`LoadedMemories`、`MemoryStore` 接口与 `SECTION_FILES` 文件名映射。
- `parser/archive.ts`：归档解析器。`extractArchives` 从对话提炼偏好/经历/日期/身份/模式条目；第 4 期 P0 极性修复——否定前置检测、疑问词过滤、不确定性降级（BC-7/8/9）；`buildSessionSummary` 每日摘要；`detectCrossTurnPatterns` 跨 ≥2 轮才判定模式。
- `parser/llm-archive.ts`（第 4 期新增）：LLM 语义归档增强层。默认关闭（`THATPERSON_LLM_ARCHIVE=true` 才启用）；输出 JSON schema 校验（type/confidence/dialog/insight/tags 全字段，任一条目非法整批拒绝）；`mergeArchives` 规则版兜底；`--mock` 不发网络；Key 优先读独立 `AAGENTDS_ARCHIVE_API_KEY`。
- `utils/ui.ts`（第 4 期新增）：CLI 表现层 UI。`logger`（info/success/warn/error/debug/title）、`showBanner`（figlet Small + 版本行）、`showStatusCard`（boxen 圆角蓝边卡片）、`startSpinner`（ora 句柄）、`ask`（inquirer 动态导入）；只做展示，不打印 Key、不外发数据。
- `utils/update-check.ts`（第 4 期新增）：更新自动检查。12h 缓存落 `thatPersonHome()/.last-update-check`；`THATPERSON_DEV=true` 或 `G:\XXFS\` 路径跳过；registry 404/网络错误/超时/解析失败/缓存写失败全部静默；数字分段版本比较。

### 3. `tests/` 测试套件（第 4 期全量 111/111）
- `helpers.ts`：测试隔离工具。`isolateHome()` 把 `THATPERSON_HOME` 重定向到临时目录；`snapshotTree()`/`assertTreeUnchanged()` 快照断言零变化。
- `isolation.test.ts`：IS-1~IS-3 隔离守卫（3 用例）。
- `security.test.ts`：SEC-1~SEC-9 安全回归（10 用例）——记忆注入边界、标签闭合转义、检索命中注入、路径穿越、Skill 注入、静态卫生（SEC-6 第 4 期口径）、离线免 Key、Skill 路径白名单、summary 注入。
- `fuzz.test.ts`（第 4 期新增）：FZ-1~FZ-5 载荷模糊（6 用例）——17 个变体（大小写/Unicode 全角/多层闭合/CRLF/混合引号）× 写盘转义与四边界；FZ-4b 摘要注入转义已闭环。
- `badcases.test.ts`：BC-1~BC-9 验收回归（9 用例）——第 3 期 6 项 + 第 4 期 P0 极性修复 3 项。
- `chat.test.ts`：Present 加载、System 消息组装顺序、`<memory>`/`<检索命中>` 边界、检索命中、技能摘要层与预算（8 用例）。
- `config.test.ts`：项目模式判定、记忆目录三档定位、配置读写（config get/set/disabledSkills）（12 用例）。
- `parser.test.ts`：偏好/经历/日期/身份/模式提取、conflict 标注、情绪基调映射、空输入健壮性（15 用例）。
- `store.test.ts`：目录结构初始化、归档格式、同日期合并、会话摘要写入、load 读取顺序、非法 section 拒绝（6 用例）。
- `cli.test.ts`（第 4 期新增）：CLI 参数解析（--version/--help/--input-file）、内部指令表、全局指令（status 真实数据/help 退出码）、工具通道（25 用例）。
- `update-check.test.ts`（第 4 期新增）：12h 缓存生效、force 绕过、跳过策略、404/网络错误/JSON 解析失败静默、版本对比（17 用例）。

### 4. `present/` Present 元认知
- `identity.md`：Agent 的身份声明（我是谁、项目归属），是 System Prompt 中人格的源头。
- `persona.md`：性格与语气设定（温暖、细腻、善于倾听的个人 AI 伴侣）。
- `behavior.md`：行为准则（回复只融入 ≤1 条相关记忆、不机械罗列、不主动打探隐私等；第 4 期新增「能力自查开关」条目）。
- `output.md`：输出习惯与格式偏好。
- `capabilities.md`（第 4 期新增）：能力清单——技能（5 出厂 + 自定义）/ CLI 指令 / 记忆能力 / 能力边界，经 `loadPresent()` 自动注入 System 供能力自省。
- 项目级 `present/` 可覆盖用户级 `~/.thatperson/present/` 同名文件（仅项目模式生效）。

### 5. `history/` 长期记忆（项目模式）
- `README.md`：记忆目录说明与数据卫生约定（五维结构、写入规范）。
- `profile/`：身份画像——`identity.md`（身份）/ `preferences.md`（偏好）/ `traits.md`（性格），按需生成。
- `timeline/`：人生节点——`milestones.md` / `important_dates.md`（重要日期用于 14 天日程层）。
- `experiences/`：经历日志——`journal.md`（动宾短语提炼的经历）。
- `insights/`：模式洞察——`patterns.md`（跨轮行为模式）。
- `sessions/`（第 4 期新增）：`/save` 会话快照——`session-<时间戳>.md`（不覆盖同名文件）。
- `session_logs/`：每日对话摘要——`YYYY-MM-DD.md`（核心话题/情绪基调/新增记忆/待跟进事项）。

### 6. `ThatPerson反馈收集/`
- `文件夹说明.md`：反馈目录使用约定。
- `Open/`：待处理反馈（当前仅 `文件夹说明.md`，两份第三期反馈已移入 Done）。
- `Done/`：已解决反馈归档——含「第三期试用反馈」「第三期反馈诊断与下期修复计划」（附第 4 期处理状态）、「提示词预设模板去留与改进建议」「记忆上下文失控分析与改进方案」「项目总结第二期」等。

### 7. `ThatPerson项目资源文件/`（项目知识库）
- `第4期-KeySpecs-20260811.md`：第 4 期硬性技术规格（R-1 交付物，S-01~S-20）。
- `第3期-KeySpecs-20260810.md`、`第3期迭代总结与架构决策-20260810.md`：第 3 期知识沉淀。
- `可用模板/`：项目报告提交模板 / Git提交说明模板 / 安全审查报告模板（Scribe 三件套依据）。
- `提示词文件夹/`：第一/二/三/四版提示词（第四版为第 4 期执行任务书）。
- `行业数据/`：AI智能体 / IT计算机 / 单身经济 行业分析报告。
- `领域相关资料/`：Agent 项目 / Agent 上下文工程 / AI Agent 记忆机制 / Skills 教程 / Agent开发团队职业规划 等方法论。
- `安全专项/`：安全测试环境搭建指南（第 3 期）/ 测试地图（第 4 期同步）/ 安全测试工具操作指南（第 4 期新增）/ 文件夹说明。
- `CLI生态相关/`：`指令相关/`、`更新检查/`、`CLI美化/` 三篇参考文档 + `说明文档/CLI生态说明清单.md`（第 4 期交付物）+ `CLI界面优化相关/CLI界面优化报告.md`（第 4 期交付物）。

### 8. `项目报告/`
- `第一期/`、`第二期/`、`第三期/`、`第四期/`：按期次归档的三件套——项目状态报告 / 安全审查报告 / Git 提交说明；第三期另含 KeySpecs、迭代总结与安全测试专项报告；第四期另含 CLI生态说明清单、CLI界面优化报告（双落点副本）。
- `目录树.md`：本文件，项目结构全景说明。

### 9. `skills/` 出厂技能库（项目级，随 npm 打包）
- `skills/README.md`：技能库说明与目录约定（用户级 `~/.thatperson/skills/` 同名技能优先覆盖出厂技能）。
- `skills/code-op/`：代码优化 / 重构 / 审查技能（SKILL.md + examples / references / templates）。
- `skills/industry-analysis/`：行业分析技能（现状 / 趋势 / 行情）。
- `skills/prompt-op/`：提示词优化技能（人设 / 系统提示词工程）。
- `skills/vault-api-bridge/`：Obsidian 知识库桥接技能（含 scripts/自动化脚本/，读取 / 搜索 / 投喂笔记）。
- `skills/warehouses-management/`：知识库整理技能（收件箱 / 分类 / 归档）。
- 代码落点：`src/skill.ts` 默认扫描包内 `skills/`（`PACKAGE_SKILLS_DIR`）；`src/cli.ts` 同时扫描 `.claude/skills/` 与包内 `skills/`；`package.json` `files` 字段保证 `dist` 与 `skills` 随 npm 打包（npm pack --dry-run = 49 文件，第 4 期亲验）。

### 10. 构建产物与依赖（gitignore，不入库）
- `dist/`：主程序构建产物（`tsc -p tsconfig.build.json`），入口 `dist/src/index.js`、`dist/src/cli.js`；顶层 `dist/index.js` 为历史遗留旧产物。
- `dist-test/`：测试编译产物（`tsc -p tsconfig.test.json`），由 `node --test "dist-test/tests/*.test.js"` 消费。
- `node_modules/`：npm 依赖（devDependencies：typescript、@types/node；dependencies：6 UI + commander，第 4 期供应链评审通过）。
