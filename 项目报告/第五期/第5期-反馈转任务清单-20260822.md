# ThatPerson 第 5 期 · 反馈转任务清单（R-1 辅助交付物）

> 日期：2026-08-22 ｜ 将 `ThatPerson反馈收集\Open\` 4 份反馈逐条转为可执行任务条目
> 对应 Key Specs：`项目报告\第五期\第5期-KeySpecs-20260822.md`（KS-1~24）
> 批次：批次一 = 数据质量/部署体验/定位话术；批次二 = 工具层/ReAct/安全收口
> 优先级：P0-立即（半天）＞ P0-主线（本周）＞ P1（批次内）＞ P2（收口）
> 完成定义：每一条验收判据通过后，对应反馈条目即可从 Open/ 移入 Done/（Done/ 同名文件不覆盖，加时间戳）。

## 一、反馈 1：《ThatPerson全局部署首次引导问题分析与改进方案-20260812.md》

| # | 反馈要点（原文提炼） | 对应 D/规格 | 批次 | 优先级 | 验收判据 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 1-1 | `~/.thatperson/` 在 `--version`/`--help` 首次引导路径不自动生成 | D9（+D15 子目录补 history/）｜KS-7 | 批次一 | P0-主线 | 全新 HOME 执行 `thatperson --version` 后目录含 config.json + present/skills/logs/history |
| 1-2 | 缺 `AAGENTDS_API_KEY` 即中断在线运行，且按文档在工作目录建 `.env` 全局安装下读不到（loadEnv 读包安装目录） | D11 resolveApiKey 同源｜KS-9 | 批次一 | P0-主线 | `resolveApiKey()` 优先级：环境变量 > config.json.apiKey > 包目录 .env；`chat()` 不再直接读环境变量 |
| 1-3 | 要求首次部署有配置向导（Key 与模型同文件、用户级目录自动生成） | D11 setup 向导｜KS-8 | 批次一 | P0-主线 | 无 Key 自动弹向导仅一次；`thatperson setup`/`wizard` 可重跑；`--version/--help/--mock`/管道不弹 |
| 1-4 | `config set` 白名单无 apiKey，用户按提示也配不上 | D11 `CONFIG_KEY_WHITELIST` + apiKey｜KS-9 | 批次一 | P0-主线 | `thatperson config set apiKey <key>` 可用且写入 config.json |
| 1-5 | `apiKeyGuidance()` 全仓无调用点（死代码） | D10｜KS-8 | 批次一 | P0-主线 | 缺 Key 启动指引指向「运行 thatperson setup」；死代码接线 |
| 1-6 | Key 明文写 config.json 需补文件权限与回显掩码 | D11 安全配套｜KS-10 | 批次一 | P0-主线 | config.json 0600（POSIX）；status/get apiKey 掩码 `sk-***abcd` |
| 1-7 | 取消「项目级/用户级」二元，统一「主目录 `~/.thatperson/` + 随身目录 `cwd/.thatperson/`（可选）」定位模型 | 部署反馈 §8｜KS-12 | 批次一 | P1 | `localThatPersonDir(cwd)` 替换 `isProjectMode()`；级联顺序测试通过；文案统一 |
| 1-8 | `history/` 已进 git 未忽略（发布卫生隐患）；npm pack 未含但需防未来误发布 | 发布卫生｜KS-24 | 批次一 | P1 | `history/` 加入 `.gitignore`；`npm pack` 核验不含 history/.env/API-key |
| 1-9 | 文档引导矛盾：README/使用说明让用户在「工作目录」建 .env | 文档同步｜KS-13 | 批次一 | P1 | 使用说明全局安装章节不再引导「工作目录建 .env」；目录表补 config.json 含 Key 掩码字段 |
| 1-10 | 用户级 present 默认为空属正常，文档未说明 | 文档补充｜KS-13 | 批次一 | P2 | 使用说明目录表说明「用户级 present 默认为空属正常，可放覆盖文件」 |

## 二、反馈 2：《ThatPerson架构优化方案-20260816.md》

| # | 反馈要点（D 编号原文） | 对应 D/规格 | 批次 | 优先级 | 验收判据 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 2-1 | D1 技能无法执行：无 Function Calling、无工具执行器、`detectToolIntent` 只认 check directory | D1/D3 工具层｜KS-16~20 | 批次二 | P0-主线 | `chat()` 带 tools + tool_choice:auto；loop.ts 三段闭环跑通 |
| 2-2 | D2 vault 桥接断链：脚本存在但无子进程执行通道 | D2｜KS-18/21 | 批次二 | P0-主线 | `read_vault_note`/`search_vault` node 原生实现；「读取某日记」命中并执行 |
| 2-3 | D3 工具意图识别覆盖极窄（读笔记/读文件/整理仓库全 null） | D3｜KS-17/20 | 批次二 | P0-主线 | 自然语言「从知识库中读取2026年7月31日的日记」→ 模型调 read_vault_note 并真实返回 |
| 2-4 | D4 同句多锚点重复归档（「最喜欢…做着自己喜欢的事情」→ 2 条） | D4｜KS-1 | 批次一 | P0-立即 | 同句同极性只产 1 条；BC-10 通过 |
| 2-5 | D5 无意义对象残片（「用户喜欢「事情」」） | D5｜KS-2 | 批次一 | P0-立即 | 占位词黑名单过滤；不再产出「用户喜欢「事情」」 |
| 2-6 | D6 不确定陈述双重归档 + 过度推断（「不确定我到底喜不喜欢看书」） | D6｜KS-3 | 批次一 | P0-立即 | 不确定/否定句子跳过经历提取；BC-11 通过 |
| 2-7 | D7 长文本（日记）完全漏报（0 条） | D7｜KS-4 | 批次一 | P0-立即 | >200 字内容模式全文归档 ≥1 条；BC-12 通过 |
| 2-8 | D8 日记内容不被在意（回复层面只回应「发日记」动作） | D8｜KS-5/6 | 批次一 | P0-主线 | 回复先回应内容再回应动作；检索段落化命中 |
| 2-9 | D9 首次部署不生成 `~/.thatperson/` | D9｜KS-7 | 批次一 | P0-主线 | 同 1-1 判据 |
| 2-10 | D10 `apiKeyGuidance()` 死代码 | D10｜KS-8 | 批次一 | P0-主线 | 同 1-5 判据 |
| 2-11 | D11 配置向导缺失、Key 无配置通道 | D11｜KS-8/9 | 批次一 | P0-主线 | 同 1-3/1-4 判据 |
| 2-12 | D12 present/ 为空不提醒 | D12｜KS-13 | 批次一 | P1 | 空 present 提醒出现；present init/show 可用 |
| 2-13 | D13 定位口径分裂：运行期「伴侣」vs 文档「管家」 | D13｜KS-14 | 批次一 | P1 | 人格句=「个人管家」；src/present 口径统一 |
| 2-14 | D14 核心比喻（API=大脑/skill=手/Markdown=记忆）写入程序并注入 System | D14｜KS-15 | 批次一 | P1 | grep 断言 src/present/package.json 无比喻（ARCHITECTURE 例外标注） |
| 2-15 | D15 `ensureConfigDir()` 子目录清单不含 history/ | D15｜KS-7 | 批次一 | P1 | 子目录清单含 history/ 或文档写明由 store 创建 |
| 2-16 | D16 Skill 扫描目录双来源（skill.ts vs cli.ts） | D16｜KS-12/21 | 批次二 | P2 | 扫描目录统一「主目录→随身目录→包内」；无 `.claude/skills` 双来源 |
| 2-17 | D17 无工具层安全测试 | D17｜KS-23 | 批次二 | P2 | tools.test.ts + SEC-10~12 通过；工具层红队结论入安全审查 |
| 2-18 | ADR-1~5 架构决策采纳（工具独立于技能 / ReAct 在编排层 / 双边界双预算 / 权限分级一等公民 / Key 与配置同源） | ADR 系列｜KS-16~21 | 批次二 | P0-主线 | 架构分层落地：src/tools/ + src/agent/loop.ts 存在并接线 |

## 三、反馈 3：《ThatPerson第四期问题核查报告-20260816.md》

| # | 反馈要点（P 编号） | 对应 D/规格 | 批次 | 优先级 | 验收判据 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 3-1 | P1 技能无法真正执行（只有发现→匹配→加载，无执行通道） | D1/D2/D3｜KS-16~20 | 批次二 | P0-主线 | 同 2-1/2-3 判据 |
| 3-2 | P2 发送日记后没在意内容本身 | D8｜KS-5/6 | 批次一 | P0-主线 | 同 2-8 判据 |
| 3-3 | P3 归档数据质量差（无意义残片/同段双条目/不确定双重归档） | D4/D5/D6｜KS-1~3 | 批次一 | P0-立即 | 同 2-4/2-5/2-6 判据 |
| 3-4 | P4 首次部署不自动生成 `~/.thatperson/` | D9/D10｜KS-7/8 | 批次一 | P0-主线 | 同 1-1/1-5 判据 |
| 3-5 | P5 缺少配置向导引导 API-key 与模型 | D11｜KS-8/9 | 批次一 | P0-主线 | 同 1-3/1-4 判据 |
| 3-6 | P6 present/ 为空时应主动提醒 | D12｜KS-13 | 批次一 | P1 | 同 2-12 判据 |
| 3-7 | P7 定位仍是「AI 伴侣」应改为「个人管家」 | D13｜KS-14 | 批次一 | P1 | 同 2-13 判据 |
| 3-8 | P8 核心概念比喻被写入程序（package.json/present/ARCHITECTURE） | D14｜KS-15 | 批次一 | P1 | 同 2-14 判据 |
| 3-9 | P9 下期方向 7 项全部未实现（present 自动填入/经历情绪化写入/wizard+reset/管家定位/重写工具调用/安全测试同步/去比喻） | M1~M5｜KS-8/11/14/15/16~24 | 批次一+二 | 滚动 | 批次一验收①~⑩ + 批次二验收①~⑪ 逐条 ✅ |
| 3-10 | 附带：chat.ts 人格句硬编码 3 句未走 present 层 | D13｜KS-14 | 批次一 | P2 | 人格句改为管家且允许 present 覆盖自定义定位 |

## 四、反馈 4：《ThatPerson解决方案与发展方向规划-20260816.md》

| # | 反馈要点（方案代号） | 对应 D/规格 | 批次 | 优先级 | 验收判据 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 4-1 | 方案 A1 工具定义层：ToolDef/ToolResult JSON Schema + 首批 8 工具清单 | D1/D3｜KS-16/17 | 批次二 | P0-主线 | tools.test.ts 列出全部 read/write 工具；未注册 name 拒绝 |
| 4-2 | 方案 A2 API 改造：Function Calling（tools + tool_choice:auto），纯文本路径保留 | D1｜KS-19 | 批次二 | P0-主线 | 111 既有测试不改一行全绿；请求体结构断言 |
| 4-3 | 方案 A3 工具执行器：参数校验/路径白名单/权限门/结果截断 | D2/D3｜KS-18 | 批次二 | P0-主线 | 路径穿越/白名单外/符号链接全拒；截断生效；danger 默认禁用 |
| 4-4 | 方案 A4 回灌闭环：ReAct 循环 + MAX_TOOL_ITERATIONS=5 + 失败恢复/认输 + 审计日志 | D1/D3｜KS-20 | 批次二 | P0-主线 | loop 测试 3 路径；审计日志落盘无 Key 明文 |
| 4-5 | 方案 A5 技能→工具桥接：SKILL.md frontmatter tools: 声明 + 动态注册 | D2/D16｜KS-21 | 批次二 | P0-主线 | vault-api-bridge tools 声明生效；SKILL.md 原文不进 System |
| 4-6 | 方案 A6 验收：示例一场景全通 + 工具层安全测试 | D17｜KS-23/24 | 批次二 | P0-主线 | 行动闭环跑通（完成定义双通道验证） |
| 4-7 | 方案 B1 规则版三闸修复（单句单条目/无意义对象/不确定不产经历） | D4/D5/D6｜KS-1~3 | 批次一 | P0-立即 | 同 2-4/2-5/2-6 判据 |
| 4-8 | 方案 B2 LLM 语义归档增强（约束 + schema 校验 + 独立 Key） | D6/D7 深化｜KS-22 | 批次二 | P1 | `--mock` 返回 null 走规则兜底；伪造条目整批拒绝 |
| 4-9 | 方案 B3/B4 内容感知：长文本段落检索 + 全文归档 | D7/D8｜KS-4/6 | 批次一 | P0-立即 | 同 2-7/2-8 判据 |
| 4-10 | 方案 C1 配置向导 + Key 并入 config.json + resolveApiKey | D10/D11｜KS-8/9 | 批次一 | P0-主线 | 同 1-2/1-3/1-4 判据 |
| 4-11 | 方案 C2 首次运行标记（configured: false）+ 目录生成 | D9｜KS-7/11 | 批次一 | P0-主线 | 同 1-1/2-9 判据 |
| 4-12 | 方案 C3 present/ 空目录提醒 + present init/show | D12｜KS-13 | 批次一 | P1 | 同 2-12 判据 |
| 4-13 | 方案 C4 验收：全新 HOME 模拟 + Key 权限/掩码回归 | D11｜KS-10 | 批次一 | P0-主线 | isolateHome 模拟验收通过；0600/掩码回归用例通过 |
| 4-14 | 方案 D1 定位统一「个人管家」（保留温暖基调，允许用户自定义） | D13｜KS-14 | 批次一 | P1 | 同 2-13 判据 |
| 4-15 | 方案 D2 去程序内比喻（grep 断言 + history 清洗） | D14｜KS-15 | 批次一 | P1 | 同 2-14 判据；history 无比喻残留 |
| 4-16 | 方案 E 五里程碑路线图（M1 工具层地基 / M2 记忆质量 / M3 部署体验 / M4 定位与 CLI / M5 安全收口） | M1~M5｜KS-16~24 | 批次一+二 | 滚动 | M1 先行（批次二 P0）；M2~M5 随批次推进逐项 ✅ |

## 五、批次汇总（Dev 取用）

| 批次 | 覆盖任务条目 | 归属 Dev | 门禁 |
| :-- | :-- | :-- | :-- |
| 批次一（先交付） | 1-1~1-10、2-4~2-15（D4~D15）、3-2~3-8/3-10、4-7/4-9~4-15 | D-1（归档/内容感知）、D-2（部署/定位/CLI）、D-4（BC-10~12 + 测试扩展） | `npm.cmd test` 全绿 + 全新 HOME 模拟验收 + 三个实测反例转正 + QA 一票否决 |
| 批次二（后交付） | 2-1~2-3/2-16~2-18、3-1/3-9、4-1~4-6/4-8/4-16 | D-3（工具层/ReAct/桥接）、D-1（LLM 归档增强）、D-4（tools.test/SEC-10~12/红队） | 批次一 PASS 后接线；行动闭环跑通 = 第 5 期完成定义 |

> 说明：同一条反馈要点可能映射到多条 Key Specs；Dev 以 Key Specs 为硬性规格，本清单仅作任务溯源与验收跟踪。反馈闭环纪律：每条验收通过后立即将对应 Open/ 反馈移入 Done/（未采纳写明理由；Done/ 同名文件加时间戳不覆盖）。
