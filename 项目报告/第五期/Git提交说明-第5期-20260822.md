# Git 提交说明 · 第 5 期

> 提交日期：2026-08-22
> 远端仓库：https://github.com/<owner>/thatperson（待 BUCISHUI 确认后填写）
> 本次提交：第 5 期迭代——归档三闸 + 长文本内容感知 + 首次部署体验闭环 + 定位管家去比喻（批次一）；工具层 + Function Calling + ReAct 编排 + 技能→工具桥接 + 新功能接线 + 安全收口 + 发布就绪（批次二）
> 建议提交信息：`feat: 第5期两批次交付（归档三闸/部署体验/工具层/ReAct/安全收口，v1.2.0）`
> 说明：本文件按模块分条，供提交时拆分或合并使用；敏感文件（.env / API-key.md / dist / node_modules / history）均不入库。
> ⚠️ 注意：本会话 `.git` 目录只读，`git add` 被 index.lock 权限拒绝（src/config.ts 当前处于「D 删除 + ?? 未跟踪」的索引异常状态）。请在真实 git 客户端执行下节命令归一后提交。

## 一、执行过程（git 命令清单，待真实 git 环境执行）

1. `git status --short --branch` —— 查看工作区与暂存区状态，确认上期基线、列出新增文件
2. `git add -A` —— 归一索引（修复 src/config.ts 的 D+?? 异常；新文件入暂存区）
3. `git status --short` —— 复核暂存区：确认无敏感文件（.env / API-key.md / history/ / dist/ / node_modules/）
4. `git check-ignore .env API-key.md history/x.md` —— 复核忽略状态
5. `npm.cmd test` —— 全量测试通过（156 用例：155 通过 / 1 跳过 / 0 失败）
6. `npm.cmd run build` —— 构建零错误
7. `git commit -m "feat: 第5期两批次交付（归档三闸/部署体验/工具层/ReAct/安全收口，v1.2.0）"`
8. `git push`（待 BUCISHUI 确认 1.2.0 发布后再执行）

## 二、变更内容（按模块分条）

### 模块一：归档质量三闸与长文本内容感知（批次一 · D-1）
- `src/parser/archive.ts`：单句单条目闸（句子+极性去重）；占位词黑名单（事情/东西/感觉等）；不确定不产经历；新增 `extractContentModeArchives`（长文本内容通道）
- `src/cli.ts`：长文本（>200 字）内容模式检测与全文归档
- `src/chat.ts`：回复指令「用户分享具体内容时先回应内容，再回应动作」；检索语料段落化 + journal 语料
- `tests/badcases.test.ts`：BC-10（同句多锚点单条目）/ BC-11（不确定不产经历）/ BC-12（长文本内容归档）
- `tests/parser.test.ts`：占位词/长文本用例

### 模块二：首次部署体验闭环（批次一 · D-2）
- `src/config.ts`：`apiKey?/configured?` 字段；CONFIG_KEY_WHITELIST 增 apiKey；`resolveApiKey()`（环境变量 > config.json > .env）；`maskApiKey()`；`resetConfig()`（仅保留 model+apiKey）；`localThatPersonDir()` 随身目录模型；ensureConfigDir 上移时机语义 + 补 history/ + 0600 写盘
- `src/setup.ts`（新）：`runSetupWizard()`（inquirer password 输入 Key，不打印不落日志；非交互入口不弹）
- `src/cli.ts`：`setup/wizard/reset` 指令；无 Key 自动弹向导（TTY 且未 configured）；config get/set apiKey 掩码；status 显示掩码 Key
- `src/present.ts`：主目录→随身目录→包内级联；`presentInit`/`presentShowText`；空 present 提醒
- `src/cli.ts`：`present init/show` 指令
- `tests/config.test.ts` / `tests/security.test.ts` / `tests/cli.test.ts`：掩码、优先级、0600、reset、present init、向导卫生、version 目录生成

### 模块三：定位管家与去比喻（批次一 · D-2）
- `src/chat.ts` / `src/report.ts` / `src/utils/ui.ts`：人格句与文案中性化「个人管家」
- `present/identity.md` / `present/capabilities.md` / `package.json`（description）：去「=大脑/=手/=记忆」比喻
- `ARCHITECTURE.md`：保留内部心智模型并标注例外（去程序内比喻，不删除架构文档语义）

### 模块四：工具层（批次二 · D-3）
- `src/tools/types.ts`（新）：ToolDef / ToolParams / ToolContext / ToolResult 契约
- `src/tools/registry.ts`（新）：注册表白名单 + `listTools`/`buildToolSpecs`
- `src/tools/guards.ts`（新）：RESULT_CHAR_LIMIT=4000；validateParams（类型/必填/enum）；assertPathAllowed（.. / 盘符混用 / NUL / realpath 复检防符号链接逃逸）；truncateResult
- `src/tools/executor.ts`（新）：统一入口——unknown-tool / danger-disabled / 参数校验 / 结果截断 / 异常捕获
- `src/tools/builtin.ts`（新）：8 工具（list_directory/read_file/read_vault_note/search_vault/search_memory/append_memory/edit_present/run_shell；run_shell 需 THATPERSON_ENABLE_SHELL=true 才注册）
- `tests/tools.test.ts`（新）：17 条（注册表白名单/门控/参数校验/路径穿越/截断/桥接/loop）

### 模块五：Function Calling 与 ReAct 编排（批次二 · D-3）
- `src/chat.ts`：返回 `ChatResult { content, toolCalls? }`；`ChatMessage` 支持 role='tool'；请求体 tools+tool_choice:'auto'；新增 `buildToolSummary`/`buildChatMessages` 导出
- `src/agent/loop.ts`（新）：MAX_TOOL_ITERATIONS=12（THATPERSON_MAX_TOOL_ITERATIONS 可调）；解析→执行→回灌→再推理；连续失败 3 次认输；审计日志只记 argsKeys；--mock 走 THATPERSON_MOCK_TOOL_CALLS 注入
- `src/cli.ts`：普通消息走 `runAgentLoop`（自动注册 builtins）
- `src/index.ts`：适配 ChatResult
- **收尾修复（CR-031，2026-08-22 晚）**：`ChatMessage` 新增 `toolCalls`；`buildChatMessages` 把 assistant 的 `tool_calls` 序列化进请求体（type:'function'）；`loop.ts` 回灌 assistant 消息时携带 toolCalls——修复真实 Key 实证触发的 DeepSeek HTTP 400「Messages with role 'tool' must be a response to a preceding message with 'tool_calls'」；新增 `tests/chat.test.ts` 两条回归（配对序列化 + 缺省行为不变）
- **收尾升级（CR-032，2026-08-22 晚）**：限制全部可配置化「给 thatperson 自由」——文件上限 2MB→50MB（`THATPERSON_MAX_FILE_MB`）、工具结果截断 4000→16000（`THATPERSON_RESULT_CHAR_LIMIT`）、ReAct 轮次 5→12（`THATPERSON_MAX_TOOL_ITERATIONS`）、system token 预算 6000/4000→16000/8000（`THATPERSON_SYSTEM_TOKEN_BUDGET/TARGET`）、检索 Top-K 8→12（`THATPERSON_RETRIEVE_TOP_K`）、单次扫描 500→2000 文件 / 深度 8→16、summary 上限 2000→6000；保留连续失败 3 次认输保险丝；BC-5 与截断测试改动态断言

### 模块六：技能→工具桥接与新功能接线（批次二 · D-3/D-2）
- `src/skill.ts`：SKILL.md frontmatter 新增 `tools:` 声明；SkillInfo.tools；扫描目录口径统一（主目录→随身→包内）
- `skills/vault-api-bridge/SKILL.md`：声明 `tools: [read_vault_note, search_vault, search_memory]`
- `edit_present` / `append_memory` 工具 handler（builtin.ts，write 权限，追加不覆盖/冲突拒绝）
- `src/parser/llm-archive.ts`：M2 增强——独立 Key AAGENTDS_ARCHIVE_API_KEY（禁止复用主 Key）、insight 语义概括、同条去重、不确定规则、schema 校验
- `src/memory/types.ts` / `src/memory/store.ts`：LoadedMemories.journal 字段

### 模块七：安全收口与发布就绪（批次二 · D-4/O-1）
- `tests/security.test.ts`：SEC-10（工具清单静态不可注入）/ SEC-11（tool_result 边界闭合）/ SEC-12（run_shell 双门控）+ 工具层红队 + KS-21 桥接
- `src/utils/update-check.ts`：REGISTRY_URL 对齐 scoped 包名（@nineteenfolk%2fthatperson）
- `package.json`：version 1.2.0；prepublishOnly 保持 build + test
- `.gitignore`：新增 `history/`（项目本地记忆不再入库）
- `ThatPerson项目资源文件\安全专项\测试地图.md` / `安全测试工具操作指南.md`：第 5 期同步（SEC-10~12 / tools.test.ts / BC-10~12 / 工具层红队）
- `README.md` / `使用说明.md`：Key 配置、目录表、命令参考、定位更新

### 模块八：反馈闭环与交付（批次二 · S-1）
- `ThatPerson反馈收集\Done\`：4 份反馈移入（架构优化方案 / 第四期问题核查报告 / 解决方案与发展方向规划 / 全局部署首次引导问题分析与改进方案，后缀 -已处理20260822）
- `项目报告\第五期\`：第5期-KeySpecs-20260822.md / 第5期-反馈转任务清单-20260822.md / ThatPerson项目状态报告-第5期-20260822.md / ThatPerson安全审查-第5期-20260822.md / Git提交说明-第5期-20260822.md

## 三、本次提交的文件

| 类别 | 文件/目录 |
| :--- | :--- |
| 新功能 | `src/setup.ts`、`src/tools/`（types/registry/guards/executor/builtin）、`src/agent/loop.ts` |
| 改造 | `src/config.ts`、`src/cli.ts`、`src/chat.ts`、`src/parser/archive.ts`、`src/parser/llm-archive.ts`、`src/present.ts`、`src/skill.ts`、`src/report.ts`、`src/index.ts`、`src/utils/ui.ts`、`src/utils/update-check.ts`、`src/memory/types.ts`、`src/memory/store.ts` |
| 测试 | `tests/tools.test.ts`（新）、`tests/security.test.ts`、`tests/badcases.test.ts`、`tests/parser.test.ts`、`tests/config.test.ts`、`tests/cli.test.ts`、`tests/chat.test.ts` |
| 资源 | `skills/vault-api-bridge/SKILL.md`、`present/identity.md`、`present/capabilities.md`、`ThatPerson项目资源文件\安全专项\测试地图.md`、`ThatPerson项目资源文件\安全专项\安全测试工具操作指南.md`、`ThatPerson反馈收集\Done\`（4 份） |
| 报告 | `项目报告\第五期\`（5 份） |
| 文档 | `README.md`、`使用说明.md`、`ARCHITECTURE.md`、`目录树.md`、`.gitignore`、`package.json` |

## 四、排除项与原因

| 排除内容 | 原因 |
| :--- | :--- |
| `API-key.md`、`.env`、`*.key` | 含明文 API Key，绝不提交（已由 .gitignore 忽略，git check-ignore 复核） |
| `history/` | 项目本地记忆（第 5 期新增忽略；记忆统一落主目录 `~/.thatperson/`） |
| `dist/`、`dist-test/` | 编译产物（构建输出），不提交 |
| `.idea/`、`.claude/`、`.codex/`、`.agents/` | IDE 与智能体本地配置，不提交 |
| `node_modules/` | 依赖目录，由 lock 文件锁定版本即可 |
| `ThatPerson项目资源文件\参考代码\`、`可用模板\项目版本规范.md`、`安全专项\第三期/第四期\` | 参考/资源类文件（未跟踪），属资源可单独决定是否入库 |

**资源文件重命名说明（待 `git add -A` 归一确认）**：本会话前已发生的资源归位动作会在暂存区显示为「D 旧路径 + ?? 新路径」：
- `CLI生态相关\CLI美化\关于ThatPerson完整的CLI美化开发第一期.md` / `指令相关\…CLI生态开发第一期.md` / `更新检查\…更新检查开发第一期.md` → 已移入 `参考代码\Done\`
- `安全专项\LLM红队报告-20260812.md` → 已移入 `安全专项\第四期\`
- `安全专项\安全测试环境搭建指南-20260810.md` → 已移入 `安全专项\第三期\`
`git add -A` 后这些移动会以 rename 形式归一；若 BUCISHUI 决定资源文件不入库，可保持忽略/单独决策。

## 五、复核结果（安全 + 质量）

- [x] `npm.cmd test` 全量通过：156 用例，155 通过 / 1 跳过（POSIX 0600）/ 0 失败
- [x] `npm.cmd run build` 构建零错误（tsc -p tsconfig.build.json）
- [x] `git check-ignore .env` / `git check-ignore API-key.md` / `git check-ignore history/x.md` 复核忽略状态
- [x] npm pack 核验：58 文件 162.5kB，含 dist/src/tools/*、dist/src/agent/loop.js、present/、skills/；不含 history/.env/API-key
- [x] 源码与文档未发现明文 Key（仅 process.env 引用与 sk-*** 占位示例）
- [ ] 真实 git 环境 `git add -A` 后 `git status` 复核（本会话 `.git` 只读，索引未归一）——待执行

## 六、影响与注意

- **运行方式变化**：普通对话消息现在会走 ReAct 工具循环（最多 5 轮）；`--mock` 下可通过 `THATPERSON_MOCK_TOOL_CALLS` 注入工具调用做离线验证。
- **首次部署变化**：任意 `thatperson` 调用（含 `--version`）都会创建 `~/.thatperson/`（config.json + present/skills/logs/history）；无 Key 首次进入对话自动弹配置向导。
- **配置变化**：`config set apiKey <Key>` 生效（白名单新增）；`thatperson setup` 向导写回 config.json（apiKey + configured:true）；`reset` 仅保留 apiKey+model。
- **技能变化**：SKILL.md frontmatter 可声明 `tools:`，触发技能时把声明的工具动态注册进本轮工具列表；Skill 扫描口径统一为主目录→随身→包内级联。
- **版本**：1.2.0（待 BUCISHUI 确认后发布）；发布后更新检查将按 scoped 包名 `@nineteenfolk/thatperson` 查询注册表。

---

> 标签：`#报告/提交说明` `#项目/ThatPerson`

---

> 所属项目网络：[[Agent 项目]] · [[项目总结第 5 期]]

# 项目本期相关
- [[ThatPerson项目状态报告-第5期-20260822]]
- [[ThatPerson安全审查-第5期-20260822]]
- [[关于ThatPerson-Agent项目第五版提示词]]
