# Git 提交说明 · 第 3 期（ThatPerson v3）

> 建议提交信息：`feat: ThatPerson v3（上下文失控修复 + Skill 系统 + 全局目录 + 全局安装）`
> 说明：本文件按模块分条，供提交时拆分或合并使用；敏感文件（.env / API-key.md / dist/ / node_modules）均不入库。

## 一、改名 ThatGirl → ThatPerson（核对闭环）
- `tests/store.test.ts`：临时目录前缀 `thatgirl-test-` → `thatperson-test-`（唯一已提交残留）。
- 核对：`src/`、`package.json`、CLI 提示语、present 自认知均无 ThatGirl 残留；远端已为 `github.com/Bucishuimym/ThatPerson`。

## 二、上下文失控修复（3a~3e）
- `src/chat.ts`：记忆回灌改四层按需注入（画像 ≤1KB / 未来 14 天日程 / 检索 Top-K≤8 / 近期三行）；`retrieveRelevant` 增加标签倒排索引、话题联想表、停用词净化、检索源=本轮+最近 2 轮；新增 `estimateTokens` / `foldSummary` / 预算常量；回复指令限定「最多融入 1 条相关记忆」。
- `src/parser/archive.ts`：经历改动宾短语提取（消除「用户去啊」）；负向偏好回溯过滤场景词（燕麦拿铁≠咖啡馆）；补全感受词表；模式判定改为跨轮（新增 `detectCrossTurnPatterns`）。
- `src/memory/store.ts`：新增标签合并（>5 条折叠）、跨会话去重、低置信度 30 天衰减、每文件软上限 100 条 + 硬上限校验（`compactArchiveFile` / `ARCHIVE_FILE_SOFT_CAP`）。
- `src/cli.ts`：summary 二次折叠接入；跨轮模式检测接入；维护最近 2 轮检索源。
- `tests/badcases.test.ts`：新增 BC-1~BC-6 回归测试（6/6 通过）。
- `tests/parser.test.ts`：模式测试改为跨轮语义。

## 三、Present / history 模板重建（防污染）
- `present/identity.md` / `persona.md` / `behavior.md` / `output.md`：指令层保留、示例层多主题中性轮换（篮球/加班/养猫/阅读/旅行）+「仅演示格式」声明；behavior 加入相关性约束。
- `history/README.md`：新增记忆目录说明（五维结构 + 数据卫生约定）。
- 清理：`history/` 下第二版咖啡示例测试数据已清空（白纸）。

## 四、Skill 调用系统（第 5 项）
- `src/skill.ts`（新增）：SKILL.md frontmatter 解析、`~/.thatperson/skills/` + 项目 `.claude/skills/` 扫描、`/名称` 斜杠触发 + `trigger_keywords` 自动触发、渐进式加载。
- `src/cli.ts`：`/` 前缀优先匹配 Skill；启动打印已发现 Skill 列表。

## 五、目录结构与全局安装（第 6/7 项）
- `src/config.ts`（新增）：`THATPERSON_HOME` 解析、`ensureConfigDir`（config.json 默认 `deepseek-v4-flash`，已存在不覆盖）、`loadConfig`。
- `src/present.ts`：用户级 `~/.thatperson/present/` 为基线、项目 `present/` 覆盖同名。
- `package.json`：新增 `"bin": { "thatperson": "./dist/cli.js" }`。
- `src/cli.ts`：shebang `#!/usr/bin/env node`；全局 `thatperson` 命令已可用（npm link + 手动补齐 cmd/ps1 shim）。

## 六、资源文件与反馈归档
- `ThatPerson项目资源文件/第3期-KeySpecs-20260810.md`（新增）：R-1 硬性规格 + 接口契约 + 验收标准 + Bad Case。
- `ThatPerson项目资源文件/第3期迭代总结与架构决策-20260810.md`（新增）：知识沉淀。
- `ThatPerson反馈收集/`：Open/ 三份已解决反馈移入 Done/（上下文失控分析、预设模板建议、项目总结第二期）。

## 影响与注意
- `tsc` 编译通过（0 错误），`node --test` 32/32 通过。
- `.env` / `API-key.md` / `dist/` / `node_modules` 仍被 `.gitignore` 忽略，不入库。
- 全局 `thatperson` 命令使用 `~/.thatperson/`（可 `THATPERSON_HOME` 覆盖）；history 始终在项目目录。