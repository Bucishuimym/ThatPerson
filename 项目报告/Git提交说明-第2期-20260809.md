# Git 提交说明 · 第 2 期

> 提交日期：2026-08-09
> 远端仓库：https://github.com/Bucishuimym/ThatGirl
> 本次提交：ThatGirl v2 —— 持续对话 CLI + Present 元认知 + 上下文工程 + 安全闭环

## 一、执行过程（git 命令清单）

1. `git status --short --branch` —— 查看工作区状态，确认第 1 期基线干净、列出新增资源文件。
2. `npm run build` / `npm test` —— 构建与 26 项单元测试全部通过后准备提交。
3. `git add` 项目相关文件：`src/`、`tests/`、`present/`、`history/`、`package.json`、`ThatGirl项目资源文件/`、`项目报告/`。
4. `git commit -m "..."` —— 创建本次迭代提交。
5. `git push -u origin master` —— 推送至远端。

## 二、本次提交的文件

| 类别 | 文件/目录 |
| :--- | :--- |
| 新功能 | `src/chat.ts`（共享引擎）、`src/cli.ts`（持续对话）、`src/present.ts`（Present 加载） |
| 元认知 | `present/`（identity / persona / behavior / output 四份预设） |
| 改造 | `src/index.ts`（复用引擎 + Present）、`package.json`（chat / chat:mock 脚本） |
| 测试 | `tests/chat.test.ts`（Present / 边界 / 检索） |
| 资源 | `ThatGirl项目资源文件/`（第二版提示词、上下文工程、记忆机制、行业报告、自动化脚本） |
| 报告 | `项目报告/ThatGirl项目状态报告-第2期-20260809.md`、`安全审查-第2期-20260809.md`、本说明 |

## 三、排除项与原因

| 排除内容 | 原因 |
| :--- | :--- |
| `API-key.md`、`.env` | 含明文 API Key，绝不提交（`.gitignore` 忽略） |
| `dist/` | 编译产物，不提交 |
| `.idea/`、`.claude/`、`.codex/` | IDE 与智能体本地配置，不提交 |
| `node_modules/` | 依赖目录，由 `package-lock.json` 锁定 |

## 四、复核结果

- `git status` 确认无敏感文件进入暂存区；`git check-ignore` 复核 `.env`、`API-key.md` 仍被忽略。
- 本次迭代全程离线验证（--mock），未调用 API，未消耗 Key。
- 26 项单元测试通过，`tsc` 零错误。

---
> 标签：`#报告/提交说明` `#项目/ThatGirl`