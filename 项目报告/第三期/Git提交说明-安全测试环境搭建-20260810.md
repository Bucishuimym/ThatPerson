# Git 提交说明 · 安全测试环境搭建（专项，不单独开期次）

> 建议提交信息：`feat: 安全测试环境搭建（提示词注入回归 SEC-1~SEC-9 + Skill 路径加固 + 搭建指南）`
> 说明：本文件按模块分条，供提交时拆分或合并使用；敏感文件（.env / API-key.md / dist/ / node_modules）均不入库。

## 一、安全回归测试套件
- `tests/security.test.ts`（新增）：SEC-1~SEC-9 九组安全用例，覆盖六面攻击面：
  - SEC-1 / 1b：记忆注入边界 + 分层注入纵深（preferences 未命中不注入）
  - SEC-2：标签闭合注入（写盘 `<`/`>` 转义）
  - SEC-3：检索命中注入（`<检索命中>` 边界）
  - SEC-4：路径穿越（非法 section / 未知归档类型拒绝）
  - SEC-5：Skill 注入（SKILL.md 仅作数据，不进 System）
  - SEC-6：静态卫生（src 无硬编码 Key、BASE_URL 白名单、零运行时依赖）
  - SEC-7：离线隔离（--mock 无 Key 可跑、不发网络）
  - SEC-8：Skill 路径白名单（穿越名与分隔符拒绝）
  - SEC-9：summary 注入（`<早前对话摘要>` 边界）
- 全部离线运行，不发起网络请求、不消耗 API Key。

## 二、Skill 路径加固
- `src/skill.ts`：`loadSkill` 增加路径白名单守卫——拒绝 `..` 穿越与路径分隔符，禁止用户输入拼接文件系统路径（安全红线 4 闭环）。

## 三、搭建指南
- `ThatPerson项目资源文件/安全测试环境搭建指南-20260810.md`（新增）：四支柱方法论、分层测试环境、六面攻击面与载荷断言方式、手工演练沙箱、常见坑、未来扩展。

## 四、项目报告
- `项目报告/ThatPerson项目状态报告-安全测试环境搭建-20260810.md`（新增）：按项目报告模板输出，采用专项任务命名，不单开期次。

## 五、记忆归档（演示数据）
- `history/experiences/journal.md` / `history/profile/preferences.md` / `history/session_logs/2026-08-10.md`：此前功能验证产生的演示记忆（打篮球/羽毛球），一并归档入库。

## 六、测试与主程序隔离（本次追加）
- `tests/helpers.ts`（新增）：`isolateHome()`（THATPERSON_HOME 重定向到临时目录 + 恢复 + 清理）、`snapshotTree()`、`assertTreeUnchanged()`。
- `tests/isolation.test.ts`（新增）：IS-1~IS-3 守卫——隔离生效、主程序完整数据链路后真实 `~/.thatperson` 零变化、restore 恢复环境变量并清理临时目录。
- `tests/chat.test.ts`：接入 `isolateHome`；Present 用例改用临时项目目录（不再依赖真实项目文件、不再触发真实 `~/.thatperson` 创建）。
- `tests/config.test.ts`：接入 `isolateHome`；env 用例改为保存/恢复原值。
- `tests/security.test.ts`：接入 `isolateHome`；SEC-5 改用隔离 home 写 Skill，不再改全局 env。
- `tsconfig.test.json`（新增）：测试编译独立输出 `dist-test/`。
- `tsconfig.build.json`（新增）：主程序构建仅 `src`，输出 `dist/`。
- `package.json`：`test` 改为 `tsc --noEmit && tsc -p tsconfig.test.json && node --test "dist-test/tests/*.test.js"`；`build/dev/mock/chat/chat:mock/report` 改用 `tsc -p tsconfig.build.json`。
- `.gitignore`：新增 `dist-test/`。
- 清理：`dist/tests` 历史遗留测试编译产物已删除；主程序构建产物仅含 `src`。
- 验证：`npm.cmd test` 49/49 通过（既有 46 + 隔离守卫 3，全离线）。

## 影响与注意
- `npm.cmd test` 全量 49/49 通过（安全套件 9 + 隔离守卫 3 + 既有功能 37）。
- PowerShell 执行策略拦截 `npm.ps1`，统一使用 `npm.cmd`。
- 沙箱/CI 中 gitignore 目录（dist）可能只读，需保证 tsc 写权限或先编译再跑测试。
