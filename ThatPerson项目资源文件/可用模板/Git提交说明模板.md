# Git 提交说明 · 第 X 期

> 提交日期：YYYY-MM-DD
> 远端仓库：https://github.com/<owner>/<repo>
> 本次提交：<一句话概括本期迭代主题>
> 建议提交信息：`<type>: <主题>（<要点一> + <要点二> + …）`
> 说明：本文件按模块分条，供提交时拆分或合并使用；敏感文件（.env / API-key.md / dist / node_modules）均不入库。

## 一、执行过程（git 命令清单）

1. `git status --short --branch` —— 查看工作区与暂存区状态，确认上期基线干净、列出新增文件
2. `git diff --cached --stat` / `git diff --stat` —— 核对暂存区与工作区差异
3. `<构建与测试命令>` —— 构建与测试全部通过后准备提交
4. `git add <项目相关文件>` —— 仅暂存与项目相关的源码、测试、资源与报告文件
5. `git commit -m "..."` —— 创建本次迭代提交
6. `git push -u origin master` —— 推送至远端

## 二、变更内容（按模块分条）

### 模块一：<模块标题>
- <变更点 1，含文件路径与一句话说明>
- <变更点 2>

### 模块二：<模块标题>
- <变更点>

## 三、本次提交的文件

| 类别 | 文件/目录 |
| :--- | :--- |
| 新功能 | `src/…` |
| 改造 | `src/…` |
| 测试 | `tests/…` |
| 资源 | `<项目名>项目资源文件/…` |
| 报告 | `项目报告/…` |

## 四、排除项与原因

| 排除内容 | 原因 |
| :--- | :--- |
| `API-key.md`、`.env` | 含明文 API Key，绝不提交（已由 `.gitignore` 忽略） |
| `dist/`、`dist-test/` | 编译产物（构建输出），不提交 |
| `.idea/`、`.claude/`、`.codex/` | IDE 与智能体本地配置，不提交 |
| `node_modules/` | 依赖目录，由 lock 文件锁定版本即可 |

## 五、复核结果（安全 + 质量）

- [ ] `git status` 确认无敏感文件进入暂存区
- [ ] `git check-ignore .env` / `git check-ignore API-key.md` 复核忽略状态
- [ ] 源码与文档未发现明文 Key（仅 `process.env.xxx` 引用与 `sk-***` 占位示例）
- [ ] 提交后已核对远端文件列表，确认敏感文件未上传
- [ ] 构建零错误（`tsc` / 等价命令）
- [ ] 全量测试通过（`npm.cmd test` / 等价命令），并注明用例数

## 六、影响与注意

- <对使用方式、配置、运行环境、目录结构的影响>

---

> 标签：`#报告/提交说明` `#项目/<项目名>`

---

> 所属项目网络：[[Agent 项目]] · [[项目总结第 X 期]]

# 项目本期相关
- [[项目总结第 X 期]]
- [[<项目名>项目状态报告-第X期-YYYYMMDD]]
- [[安全审查-第X期-YYYYMMDD]]
- [[关于<项目名>-Agent项目第X版提示词]]
