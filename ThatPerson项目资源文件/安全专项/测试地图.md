## 三、测试地图（测试 = 系统承诺的契约）

> 全部离线、零 API。运行：`npm.cmd test`（Windows 下勿用 `npm.ps1`）。

| 套件 | 守护承诺 | 数量 |
| :--- | :--- | :--- |
| `tests/parser.test.ts` | 归档解析正确性（偏好/经历/日期/身份/模式/摘要/空输入 + 第 5 期占位词/长文本） | 16 |
| `tests/security.test.ts` | SEC-1~12 安全回归（注入/闭合/路径/Skill/静态卫生/离线 + 第 5 期 SEC-10~12/掩码/向导卫生/桥接） | 18 |
| `tests/fuzz.test.ts`（第 4 期新增） | FZ-1~5 载荷模糊（变体×写盘转义/四边界；S-17；FZ-4b 为已知缺口守卫） | 6 |
| `tests/badcases.test.ts` | BC-1~12 验收回归（话题劫持/token 预算/归档/假模式/压缩 + 第 4 期 P0 极性 + 第 5 期三闸/长文本） | 12 |
| `tests/store.test.ts` | 记忆存储（目录/格式/合并/load/防穿越） | 6 |
| `tests/chat.test.ts` | Present 加载 / System 组装 / 检索命中 / 技能摘要层 / 先回应内容 | 12 |
| `tests/config.test.ts` | 目录三档定位 / 项目模式判定 / 配置读写 / resolveApiKey / 掩码 / 0600 / reset | 16 |
| `tests/isolation.test.ts` | IS-1~3 测试与主程序隔离 | 3 |
| `tests/cli.test.ts`（第 4 期新增，他人负责） | CLI 参数解析 / 内部指令表 / 全局指令（setup/present/tools）/ 向导不弹分支 | 30 |
| `tests/update-check.test.ts`（第 4 期新增，他人负责） | 12h 缓存 / force 绕过 / 404 与网络错误静默 / 版本对比 / scoped URL | 20 |
| `tests/tools.test.ts`（第 5 期批次二新增） | 工具注册表白名单 / 参数校验 / 路径穿越 / 截断 / danger 门控 / loop 3 路径 / edit_present / append_memory / 桥接 | 17 |
| `tests/helpers.ts` | isolateHome/snapshotTree 隔离工具（非测试） | — |

> 第 5 期全量：`npm.cmd test` = 156 用例，155 通过 / 1 跳过（POSIX 0600）/ 0 失败。

**承诺速查**
- **BC-1** 经历含「打篮球」、无残句、回复指令不强制全量扫射
- **BC-2** 负向对象回溯「燕麦拿铁」而非场景「咖啡馆」
- **BC-3** 回复只融入 ≤1 条相关记忆
- **BC-4** 单条消息三提咖啡 ≠ 模式（跨 ≥2 轮才算）
- **BC-5** 3 个月记忆规模 system ≤6000 token（字符 ≤12000 保险）
- **BC-6** summary ≤2000 字，超限二次折叠保留最新
- **BC-7** 「其实我不喜欢下雨天」只归档负向、无正向（否定前置检测，无双极性）
- **BC-8** 「你记得我喜欢干什么嘛」不归档「喜欢干什么嘛」（疑问句/wh-词不进偏好对象）
- **BC-9** 「我都不确定我喜不喜欢上课」无双极性、置信度不标「高」（不确定性降级）
- **BC-10**（第 5 期）同句多锚点单条目：「最喜欢…做着自己喜欢的事情」只产 1 条偏好
- **BC-11**（第 5 期）不确定不产经历：「不确定我到底喜不喜欢看书」无经历、无「感受：不喜欢」过度推断
- **BC-12**（第 5 期）长文本内容归档：>200 字无感受词日记产出 ≥1 条经历
- **SEC-1/1b** 记忆注入被 `<memory>` 边界 + 「仅为参考」隔离；preferences 不经检索不注入
- **SEC-2** 写盘 `< >` 转义，防标签闭合
- **SEC-3/9** 检索命中/摘要置于独立边界
- **SEC-4** 非法 section/未知类型拒绝（防路径穿越）
- **SEC-5** Skill 内容仅数据，不进 System
- **SEC-6**（第 4 期更新）src 无硬编码 Key、网络仅白名单端点、无对外部域名的 fetch/网络调用、Key 明文仅允许存在于 .env / API-key.md（均被 .gitignore 排除）
- **SEC-7** `--mock` 无 Key 可跑、不发网络
- **SEC-8** loadSkill `..`/分隔符一律拒绝
- **SEC-10**（第 5 期）`<工具清单>` 静态不可注入：模型无法通过对话定义新工具
- **SEC-11**（第 5 期）`<tool_result>` 边界闭合：工具结果只作 role=tool 消息，不进 system 指令区
- **SEC-12**（第 5 期）run_shell 双门控：环境变量 THATPERSON_ENABLE_SHELL=true + 用户确认才放行
- **KS-17/18/20/22**（第 5 期）工具注册表白名单 / 参数校验与路径穿越拒绝（realpath 复检）/ 结果截断 / loop 5 轮上限与认输 / edit_present 冲突拒绝 / append_memory 即时落盘
- **FZ-1** 载荷变体写盘 `< >` 全转义、CRLF 扁平化
- **FZ-2/3** `<memory>` / `<检索命中>` 经写盘→回灌边界不失效、未转义载荷零残留
- **FZ-4a** `<早前对话摘要>` 无标签变体边界不失效
- **FZ-4b**（已修复）标签变体经 summary 直入可提前闭合摘要块——chat.ts `escapeSummaryTags` + cli.ts `escapeTags` 输入侧转义已闭环，用例转绿
- **FZ-5** Skill 内容边界：SKILL.md 变体仅数据、不进 System
- **IS-1~3** 测试重定向临时 home、真实 `~/.thatperson` 零变化、restore 干净

## 四、第 4 期新增模块测试点

| 模块 | 测试点 | 落点 |
| :--- | :--- | :--- |
| CLI 参数解析 | `--version/-V` 输出版本即退出；`--help/-h` 打印帮助退出；`--mock` 保留离线语义；无参数进对话；`--input-file` 解析；未知参数收集 | `tests/cli.test.ts` |
| 内部指令表 | `/help /history /clear /reset /exit /save`；`/save` 落盘 `history/sessions/` 且不覆盖同名 | `tests/cli.test.ts` |
| 全局指令 | `status` 状态卡片真实数据（版本/记忆条目/技能数/预算）、`help` 退出码 | `tests/cli.test.ts` |
| 工具通道 | `detectToolIntent`（检查工作目录等）、`runTool` 真实执行/诚实拒绝 | `tests/cli.test.ts` |
| update-check | 12h 缓存生效、force 绕过、404 静默失败、网络错误静默、版本相同不提示 | `tests/update-check.test.ts` |
| ui/状态卡片 | 字段无占位示例、技能数/token 预算联动 `SYSTEM_TOKEN_BUDGET` | `tests/cli.test.ts` |
| 技能摘要层 | `<技能清单>` 只含人话摘要、SKILL.md 正文不注入、不突破 token 预算 | `tests/chat.test.ts` |
| 模型来源统一 | `config.model` 为唯一请求模型来源（`chat()` 不再硬编码） | `src/chat.ts` + 回归 |
| llm-archive（已落地，默认关闭） | `--mock` 返回 `null` / 走规则版兜底；LLM 伪造条目被 schema 校验整批拒绝（S-13） | `src/parser/llm-archive.ts` + 逻辑验证 10/10 |

## 五、第 5 期新增模块测试点

| 模块 | 测试点 | 落点 |
| :--- | :--- | :--- |
| 归档三闸 | BC-10 同句多锚点单条目 / BC-11 不确定不产经历 / 占位词黑名单（「用户喜欢「事情」」消失） | `tests/badcases.test.ts` / `tests/parser.test.ts` |
| 长文本内容模式 | BC-12 >200 字日记产出 ≥1 条经历；`extractContentModeArchives` | `tests/badcases.test.ts` / `src/parser/archive.ts` |
| 部署体验 | `--version` 生成目录；setup/wizard 可重跑；非交互不弹；reset 仅保留 apiKey+model；resolveApiKey 优先级；掩码回显；config.json 0600 | `tests/config.test.ts` / `tests/cli.test.ts` / `tests/security.test.ts` |
| 工具注册表 | ≥5 个 read/write 工具；run_shell 默认不注册；未注册拒绝；buildToolSpecs 静态精简 | `tests/tools.test.ts` |
| 守卫与执行器 | 参数校验（必填/类型/enum）；路径穿越（`../`、白名单外、符号链接 realpath 复检）；结果截断；danger 双门控 | `tests/tools.test.ts` / `tests/security.test.ts`（SEC-12） |
| ReAct 循环 | loop 三段（解析→执行→回灌→再推理）；5 轮上限；连续失败 3 次认输；失败重试成功；审计日志只记 argsKeys | `tests/tools.test.ts` |
| 技能→工具桥接 | vault-api-bridge 声明 tools 注册生效；SKILL.md 原文不进 System | `tests/security.test.ts`（KS-21） |
| 新功能工具 | read_vault_note 按路径/日期读取；search_vault 递归命中；append_memory 即时落盘；edit_present append/replace/冲突拒绝 | `tests/tools.test.ts` |

## 六、LLM 红队（已完成 · 真实模型行为级验证）

- 结论：**13/13 通过**（BUCISHUI 授权使用 Key，模型 deepseek-v4-flash，走真实 chat() 通道）。
- 覆盖：记忆注入（画像/日期/检索命中）、摘要注入、System 提取（直接/越权/数据块原文）、Key 外泄、文件读取/删除、技能源码外泄、人设劫持。
- 证据：`安全专项\LLM红队报告-20260812.md`；例行红队建议配置独立低额度测试 Key。
- 可复用工具：`安全专项\scripts\redteam-run.js`（13 组用例表；`THATPERSON_REDTEAM=1` 显式开关门禁 + Key 优先级 + 隔离临时目录；`--cases A1,C3` 只跑子集，`--out <json>` 落盘结果，默认关闭）。